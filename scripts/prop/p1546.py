"""ITU-R P.1546-6：点对面场强预测（30–4000 MHz）—— **未实现，且是有意不实现。**

05 方案 6.8 为「超短波覆盖呈现」选的是 P.1546，理由是覆盖热力图是点对面、
要快，而 P.1546 是经验曲线法，正对此场景。这个选型没有变。

为什么这里是空的
----------------
P.1546 的核心是**官方场强曲线表**：对 100 / 600 / 2000 MHz 三个频率，
在 8 个发射天线有效高度（10/20/37.5/75/150/300/600/1200 m）× 约 30 个距离 ×
若干时间百分比上给出场强值，再按频率、高度、距离、时间四维插值。
这套表是 ITU 的官方数据，**离线拿不到**。

`CLAUDE.md` 写明「不确定的来源、协议、型号一律标注待确认，**不得编造**」。
凭记忆或凭拟合造一套数出来，写上 P.1546 的名字，正是最典型的蒙混过关——
外表完全正常，误差无从察觉。所以这里**宁可空着**。

现在覆盖计算用什么
------------------
`scripts/planning/viewshed.py` 的覆盖计算调用 `propagation.vuhf_path_loss`，
也就是走 **P.1812**（点对点、逐点地形剖面）。代价是比经验曲线法慢，
收益是物理依据明确、与链路可行性判据同源，不会出现「覆盖说通、
可行性说不通」的自相矛盾。实测单站 60 km 半径约 1.5 s、180 km 约 4 s，
当前规模可以接受。

要补上 P.1546 需要什么
----------------------
1. P.1546-6 附件 1 的场强曲线数据（表 1–24），由合作方提供或从 ITU 获取；
2. 数据落 `data/raw/itu/`，按本项目「原件入库 + 标注来源」的规矩处理；
3. 本模块实现四维插值与陆地/海面路径、杂波、地形余隙角修正；
4. 与 P.1812 在若干典型路径上交叉核对，差异写进审查报告。

在此之前，任何声称「覆盖用的是 P.1546」的说法都不成立。
"""


def field_strength(*args, **kwargs):
    raise NotImplementedError(
        "P.1546 未实现：官方场强曲线表离线不可得，按 CLAUDE.md 不得编造。"
        "覆盖计算当前走 P.1812，见本模块文件头。"
    )
