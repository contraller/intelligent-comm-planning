# 工作交接

> 本文件记录**代码与数据线**（`scripts/` `data/` `docs/design/`）的工作进展。
> 每完成一段工作追加一节，最新的在最上面。
> 文档线（SRS 章节）的进展见 contraller 的记录。

---

## 2026-09-16 · 移除汇报出图代码

负责人：yoii1（Claude Code）

### 做了什么

汇报出图属于汇报线的工作，不属于代码与数据线，按约定从本仓库移除：

| 删除 | 体积 |
|---|---|
| `scripts/gen_visualizations.py` | 797 行 |
| `docs/visualization/`（19 张 PNG + `map_overview.html` + `summary.json` + `README.md`） | 4.2 MB |

**保留 `progress_report/week1.pptx`。** 已确认该 pptx 内嵌了 13 张图片（1.9 MB），
图片打包在文件内部，删除 `docs/visualization/` 不影响汇报稿的正常打开与展示。

删除原因不止是分工：这份出图代码依赖 `matplotlib` / `numpy` / `pandas` / `rasterio`
四个第三方包，与 CLAUDE.md「只用标准库」冲突；且它读取的
`data/imported/.../raw/*.tif` 已随原始解压件删除，输入缺失时会**静默跳过**地形类图表
（`terrain_available: false`）而不报错，留在仓库里反而容易误导。

### 结果

- 删除后 `gen_test_data.py`、`gen_algorithm_design_data.py`、`validate_data.py`
  全部通过，0 ERROR / 0 WARNING，退出码 0；`tests/` 三个用例全部 PASS。
- 已确认除 `工作交接.md` 外无其他文件引用被删内容，不存在断链。
- 仓库中的算法与数据代码恢复为**纯标准库**：`scripts/` 下仅剩
  `terrain.py`、`propagation.py`、`gen_test_data.py`、`gen_algorithm_design_data.py`、
  `prepare_geodata.py`、`validate_data.py`、`datapaths.py` 与 `geo/` 三个读取模块。
  唯一的第三方依赖只剩 `algorithm-service/requirements.txt` 的 HTTP 层（待决定）。

### 说明

历史版本仍保留在 git 中，如需找回：`git show 857c602:scripts/gen_visualizations.py`
（或从 `f9392c7` 及更早的提交检出 `docs/visualization/`）。

---

## 2026-09-16 · 规划区西移并切换到真实地形

负责人：yoii1（Claude Code）

### 做了什么

上一节提出的 bbox 问题经确认走**方案 B**：在已下载的数据范围内把规划区西移 20 km，
不要求重新采集。同时把地形源从合成地形切换到真实 DEM —— 两件事都会触发全量数据重生成，
合并做只折腾一次。

| 项 | 改前 | 改后 |
|---|---|---|
| 规划区 bbox | `114.2724, 36.7610, 115.6276, 37.8390` | `114.0465, 36.9110, 115.4045, 37.9890` |
| 地形源 | `SyntheticTerrain`（合成） | `DemTerrain`（真实 DEM + 地物栅格） |
| 规划区高程 | 17–526 m，中位 35 m | **16–1616 m** |
| 坡度 >15° 占比 | 0.7% | **8.7%** |

具体改动：

1. **`scripts/terrain.py` 重写**。新增 `DemTerrain`（基于 `GridRaster` 读真实网格）与
   `default_terrain()` 工厂：真实数据存在则用，缺失自动回退合成地形并给出提示。
   `SyntheticTerrain` 保留，作为无数据环境的回退与冒烟测试用。
   **对外接口未变**（`elevation` / `slope_deg` / `landcover` / `profile` /
   `haversine_m` / `los_clearance`），`propagation.py` 一行未改。
2. **地物编码归并**。真实地物是 11 类数值编码（10 树木 / 40 农田 / 80 永久水体 …），
   而 `propagation.py` 的损耗表以「林地/草地/耕地/建成区/裸地/水域」为键。
   在 `DemTerrain.landcover()` 内做一次映射，使真实数据接入后传播模型无需改动。
3. **任务区重新布置**。原四个任务区按旧框设定，现改为覆盖山地、山前过渡与平原三类地形
   （太行山区 / 山前过渡区 / 中部平原区 / 东部平原区），使部署与传播计算落在有区分度的地形上。
4. **消除 bbox 硬编码**。`validate_data.py` 与 `prepare_geodata.py` 改为从
   `terrain.BBOX` / `terrain.BBOX_DATA` 取值，不再各自硬编码，避免再次漂移。
5. **同步下游**。`algorithm-service/app/candidate_sites.py` 的 `DEFAULT_AREA_BBOX`、
   `docs/api/sample_requests/candidate_sites_request.json` 的 `area_bbox` 与
   `elevation_range_m`、`docs/api/sample_responses/candidate_sites_response.json` 均已更新。
6. **15 个 schema 模板全部与实际输出对齐**（CLAUDE.md 要求的三处同步）。

### 结果

- `gen_test_data.py`、`gen_algorithm_design_data.py`、`validate_data.py` 全部通过，
  **0 ERROR / 0 WARNING**，退出码 0。
- `tests/test_candidate_sites.py` 三个用例全部 PASS。
- 真实 DEM 载入 0.2 s；沿 37.45°N 实测：114.08° 高程 1201 m 坡度 24.6° 林地，
  115.38° 高程 23 m 耕地 —— 地形与地物均来自真实数据。
- 候选点可部署数 79/120，筛除原因分布合理（距路 29、间距 9、坡度 4、地物 2）。
- 数据规模仍全部达标：节点 156（短波 52 / 超短波 104）、链路 500、通联需求 411、
  链路状态记录 2500、故障案例 240；必要通联关系 69 条两端全部连通。

### 过程中发现并修掉的两个问题

1. **样例请求的 `elevation_range_m` 下限 50 m 会误杀候选点**。合成地形整体偏高，
   这个下限一直没暴露；真实平原只有 23–35 m，直接滤掉 62 个候选点。已改为 20 m。
2. **`fault_rules_template.csv` 表头与实际输出不符**（模板来自 `d1f5aee`，
   数据来自 `563dc68`，非本次引入）。已按实际输出同步。

### 没有做的

- ~~`scripts/gen_visualizations.py` 已失效~~ **已于 2026-09-16 连同 `docs/visualization/` 一并删除**
  （汇报出图属于汇报线的工作，不在代码与数据线范围内），见本文件最上一节。
- **西侧无缓冲**。规划区西边界与 DEM 西边界重合，贴西边界链路的遮挡计算偏乐观。
  如需严格处理，需补采西延约 20 km 的 DEM（约 3 MB）。
- 真实设备型号仍未并入，`gen_test_data.py` 仍输出 `CONSTRUCTED`（表头与约定不一致，需先对齐字段）。
- 故障知识图谱三元组（500–2000 条）仍为空。
- `algorithm-service/requirements.txt` 引入 fastapi / uvicorn / pydantic，
  与 CLAUDE.md「只用标准库」冲突；核心算法本身是纯标准库，依赖只在 HTTP 层。待决定是否换成 `http.server`。

---

## 2026-09-16 · 导入数据预处理并入库

负责人：yoii1（Claude Code）

### 做了什么

数据采集同学 9/11 交付的 `data.zip`（643 MB，解压 2.06 GB）此前只存在于做导入的那台机器上，
`.gitignore` 排除了 `raw/` 与 `source/*.zip`，未同步到其他机器。本次把它处理成可入库的形式。

1. **删除解压出的 41 份 PDF**（211 MB）。均为厂商版权资料（Motorola、Barrett、Harris、Yaesu 等），
   不适合公开分发；原件完整保留在 `data.zip` 内，仓库中只留 `docs_index.csv` 索引。
2. **写了三个纯标准库的地理数据读取模块**（`scripts/geo/`）：
   - `tiff.py` —— 最小 GeoTIFF 读取器，含手写的 TIFF 变体 LZW 解码（early change、9–12 位变长码），
     支持条带与分块两种布局
   - `shapefile.py` —— 最小 Shapefile / DBF 读取器，PolyLine 几何 + 属性表
   - `grid.py` —— 网格读取与经纬度 O(1) 采样
3. **`scripts/prepare_geodata.py`** 一次性转换，产出进仓库的 28 MB：

   | 数据 | 原始 | 入库 | 处理 |
   |---|---|---|---|
   | DEM | 17.2 MB LZW 分块 GeoTIFF | 20 MB | 解码平铺 + gzip |
   | 地物 | 356.7 MB **未压缩** GeoTIFF 10 m | 4.1 MB | 3 倍降采样至约 28 m + gzip（压缩 87 倍） |
   | 道路 | 49.3 MB Shapefile（GBK） | 4.9 MB | 裁框、仅留车行等级、属性精简、转 UTF-8 + gzip |
   | 故障案例 / 资料索引 | — | 64 KB | 去 BOM |

4. **`data/raw/README.md`** 记录来源、处理方式、读取方法与待确认事项。
5. 原始 `data.zip` 移回本机桌面；解压出的 `raw/` 已删除（可从 zip 重新解出）。

### 结果

- 2.06 GB → **28 MB**，最大单文件 20 MB，远低于 GitHub 100 MB 限制，无需 Git LFS。
- 读取零第三方依赖，符合 CLAUDE.md「只用标准库」。已验证：
  `dem.sample(114.30, 37.30) -> 263 m`、`lc.sample(...) -> 30 草地`、越界返回 `None`。
- DEM 全幅高程 10–1676 m，NoData 0 个，地理配准与边界校验通过。
- 平铺 gzip 加载 0.1 s，原 LZW GeoTIFF 需 5.5 s（多 1.4 MB 换 55 倍速度）。
- 本次未改动数据生成逻辑，`gen_test_data.py` + `validate_data.py` 复跑仍为 0 ERROR / 0 WARNING。

### 发现的问题：规划区 bbox 选错了（**需要决策**）

我在 9/8 定规划区时判断「一个框内同时有太行山 1000–1800 m 与华北平原」，
真实 DEM 到位后实测，**这个判断是错的**：

| 经度带 | 高程中位 | 最大 | >500 m 占比 |
|---|---|---|---|
| **114.047–114.272（外扩缓冲带）** | **396 m** | **1526 m** | **33%** |
| 114.272–114.498 | 132 m | 527 m | 0% |
| 114.498 以东 | 30–53 m | ≤152 m | 0% |

太行山主脊全部落在 `114.047–114.272` 这条**外扩缓冲带**内，而规划区西边界正是 `114.2724`，
恰好卡在山脚下，往东全是华北平原。当前规划区实测：高程 18–524 m、中位 35 m、
坡度中位 2.2°、坡度 >15° 仅 0.7%。

影响：候选点筛选的坡度约束几乎筛不掉任何区域；SR-4.2.3 要求的地形遮蔽不规则覆盖
在平原上体现不出来，超短波覆盖会接近规则圆形 —— 而软需明确要求不得用圆形近似。

三个选项（已用真实 DEM 实测）：

| 方案 | 高程 | >500 m | 坡度 >15° | 代价 |
|---|---|---|---|---|
| A 维持现状 | 18–524 m | 0.0% | 0.7% | 无，但地形基本不起作用 |
| **B 规划区西移至现有 DEM 最西端**<br>`114.047–115.402, 36.940–38.018` | **16–1616 m** | **7.2%** | **8.7%** | 不用重下数据；西侧无 20 km 缓冲 |
| C 以主脊为中心重选（中心约 114.45, 37.30） | — | — | — | 现有 DEM 只覆盖 80%，需重下 DEM/地物/道路 |

我倾向 **B**：不用让同学重新采集，地形起伏立刻可用。代价是西边界外没有缓冲，
贴着西边界的链路拿不到更西侧的遮挡数据，覆盖会偏乐观 —— 但西边界外就是山西山区，
真实任务里本来也不会把节点部署到那边。若要彻底干净则选 C，需重下约 20% 的数据。

**此项需你确认后我再改。** 改 bbox 会触发节点重新布设与全量数据重生成，
并需同步 `docs/design/01_数据字典.md`、`docs/design/02_数据采集任务单.md` 与 `data/raw/README.md`。

### 没有做的

- **规划区 bbox 未改**，等上述决策。
- **真实设备型号未并入**。`data/raw/equipment_docs/` 仍是示例行，
  `gen_test_data.py` 仍输出 `CONSTRUCTED`。采集到的 16 个设备型号与 77 个天线型号
  表头与现有约定不一致（ID 形如 `HF001` 而非约定的 `DM-0001`，且存在空字段），
  并入需先对齐字段并同步三处定义，会触发全量数据重生成，改动较大，单独处理。
- **`terrain.py` 仍使用 `SyntheticTerrain`**。真实 DEM 的读取能力已具备（`GridRaster`），
  但未切换默认地形源 —— 切换会改变全部生成数据，应与 bbox 决策一并进行。
- 故障知识图谱三元组（500–2000 条）仍为空，是本线的欠账。

---

## 2026-09-08 · 统一枚举口径

负责人：yoii1（Claude Code）

### 做了什么

上一节第五部分提出的跨线枚举冲突已确认结论：**沿用当前代码口径**。
据此改写 `data/dictionary/data_dictionary.md` 的「通用枚举」一节：

| 概念 | 改前 | 改后 |
|---|---|---|
| 网络类型 | `network_type`：`HF` / `VHF` | `device_class`：`HF` / `VUHF` |
| 节点类型 | `node_type`：`fixed` / `mobile` / `relay` | `node_role`：`TASK` / `FIXED_STATION` / `RELAY` / `CANDIDATE`，另有 `mobility`：`FIXED` / `VEHICLE` / `MANPACK` |
| 状态 | `status`：`normal` / `degraded` / `fault` / `offline` | `status`：`NORMAL` / `FAULT` / `OFFLINE`；`degraded` 归入 `affect_state` |

同时补齐了该文件原本缺失的枚举（`link_state`、`affect_state`、`impact_level`、
`fault_type`、`interference_type`、`task_priority`）、布尔与缺失值写法、单位约定，
并注明字段语义的权威说明在 `docs/design/01_数据字典.md`。

原有「待确认事项」保留未动，另把 `source` / `remark` 可追溯性字段一并列入待确认。

### 结果

- 枚举取值已与 `scripts/validate_data.py` 的 `ENUMS` 逐项交叉核对，7 个枚举字段全部一致。
- 仓库内 `network_type` / `node_type` / 裸 `VHF` 已无残留
  （`data_dictionary.md` 中保留一处，是说明为何取 `VUHF` 的正文）。
- `data/schemas/*_template.csv` 与 `scripts/gen_test_data.py` 本就是当前口径，未改动。
- 数据未重新生成（本次只改文档），`gen_test_data.py` + `validate_data.py` 复跑仍为
  0 ERROR / 0 WARNING，与已提交数据零差异。

### 没有做的

上一节「四、没有做的」中的各项**均未推进**，本次只做枚举口径统一。

---

## 2026-09-08 · 第 1 周：数据字典、接口文档、基础测试数据集

负责人：yoii1（Claude Code）
对应计划：《项目安排》第 1 周「确定数据和接口」，交付物为数据字典、接口文档、基础测试数据集、模块接口关系说明。

### 一、做了什么

#### 1. 确定规划区域

原计划未指定区域，而 DEM、地物、道路三类数据全部按区域下载，区域不定则数据采集无法开工。已定：

| 项 | 值 |
|---|---|
| 区域 | 太行山东麓（河北邢台西 — 山西左权），中心 `114.95, 37.30` |
| 尺寸 | 120 km × 120 km，UTM 50N / EPSG:32650（不跨带） |
| 规划 bbox | `114.2724, 36.7610, 115.6276, 37.8390` |
| 数据下载 bbox（外扩 20 km） | `114.0466, 36.5814, 115.8534, 38.0186` |

选此区域的原因：一个框内同时有太行山（1000–1800 m）与华北平原（50–100 m），
短波跳距环带与超短波地形遮蔽都能出效果。纯平原区域两者算出来都接近圆形，
而软需 SR-4.2.3 c 明确要求覆盖不得用规则扇形/圆形近似。

**下载必须用外扩框**。少下这 20 km，规划区西边缘的链路拿不到太行山主脊的遮挡数据，
覆盖会算得偏乐观，而西边缘恰好是山地。

#### 2. 交付文档（`docs/design/`）

| 文件 | 内容 |
|---|---|
| `01_数据字典.md` | 全局约定（坐标系、单位、ID 规则、时间格式、全局枚举、空值约定）+ 28 类实体字段定义 |
| `02_数据采集任务单.md` | 网上采集部分的任务分解，含数据源、字段表头、单位换算、验收标准，可直接转交 |
| `03_接口文档.md` | 算法后端 ↔ GIS 前端 HTTP 接口，覆盖 SR-4.2 / SR-5 / SR-6 全部接口 |
| `04_模块接口关系说明.md` | 模块职责边界、依赖关系图、两条主流程时序图、接口实现节奏表 |

#### 3. 代码（`scripts/`）

| 文件 | 说明 |
|---|---|
| `terrain.py` | 地形引擎：高程、坡度、地物、通视（含 4/3 地球曲率）、地形剖面 |
| `propagation.py` | 传播模型与链路预算：超短波刃形绕射、短波地波 + NVIS、噪声底与 SNR 余量 |
| `gen_test_data.py` | 测试数据生成，种子 `SEED = 20260908` |
| `validate_data.py` | 数据校验，7 类检查，退出码 0/1 |
| `datapaths.py` | 数据集简称到仓库路径的映射（后续调目录只改这里） |

全部纯标准库，无第三方依赖。

#### 4. 基础测试数据集（`data/synthetic/` `data/fault/` `data/dictionary/`）

| 数据 | 实际 | 口径要求 | 状态 |
|---|---|---|---|
| 节点 | 156（短波 52 / 超短波 104） | 短波 ≥50、超短波 ≥100 | ✅ |
| 网络拓扑链路 | 500（可用 480） | 150 节点以上，200–500 链路 | ✅ |
| 任务通联关系 | 303 条 / 6 个场景 | 5–10 组，每组 30–100 条 | ✅ |
| 链路状态记录 | 2500 | ≥1000 | ✅ |
| 频率资源 | 94（短波 46 / 超短波 48） | 每类 20–50 | ✅ |
| 干扰源 | 16 | 10–20 | ✅ |
| 故障案例 | 240（四类各 60） | 4 类，每类 50–100 | ✅ |
| 现象字典 | 15 条 | —— | ✅ |
| 故障场景 | 5 个（含 1 个复合场景） | —— | ✅ |

### 二、结果与验证

```
python3 scripts/gen_test_data.py     # 全部规模达标
python3 scripts/validate_data.py     # 0 ERROR，0 WARNING，退出码 0
git status                            # 重新生成后与已提交数据零差异
```

**数据可复现已验证**：固定种子重跑，输出与仓库中已提交的数据逐字节一致。

校验脚本覆盖 7 类检查：引用完整性（12 组外键）、枚举与布尔合法性（12 个枚举字段 + 6 个布尔字段）、
数值范围、拓扑连通性、通联需求可满足性、标签分布、故障场景适配性。

### 三、几个关键决策，接手时需要知道

1. **拓扑不是取距离最短的 500 条链路**。
   第一版那样做，全网清一色 `EXCELLENT`，SR-6.2 无法从链路指标挖掘状态判定规则，
   SR-5 也没有弱链路可供故障分析。现在改成：先用并查集建连通骨架保证路由有解，
   再按状态配额分层抽样。当前分布 `EXCELLENT=250 / GOOD=140 / WARNING=80 / FAULT=30`，
   链路状态记录为 `1343 / 649 / 343 / 165`。**改动 `gen_links()` 时要保住这个分布。**

2. **必要通联关系的连通性是硬性前提**。
   SR-4.2.4 要把「必要通联关系的端到端连通率」作为硬约束写进目标函数。
   若这些通联关系两端本就不在同一连通分量，多目标求解一开始就无解。
   校验脚本第 5 项专门查这一条，当前 43 条必要需求两端全部连通。

3. **链路余量用 SNR 而非接收灵敏度**。
   灵敏度余量在短距离链路上动辄 60–70 dB，无法区分链路优劣；
   SNR 余量才与误码率、链路状态直接相关。噪声底按 ITU-R P.372 口径，
   短波取外部噪声系数 40 dB、超短波取 8 dB。

4. **合成地形已按太行山东麓标定**，DEM 到位前的占位实现：
   西部山区坡度均值 19°、最大 37°，东部平原 1.2°，坡度约束能筛掉 22% 的面积；
   高程 59–1919 m。若地形过于平坦，候选点筛选的坡度约束会形同虚设。

5. **传播模型的物理表现已核对**：
   短波地波按 ITU-R P.368 平均地质曲线标定（40 km 可用 / 60 km 预警 / 120 km 失效），
   NVIS 天线 0–300 km 全程可用；超短波 VHF 50 MHz 绕山后仍为 `GOOD`（波长 6 m，
   菲涅尔区大，绕射损耗仅约 9 dB），同路径 UHF 400 MHz 为 `FAULT`。
   这些都是占位实现的合理表现，不是缺陷。

### 四、没有做的（明确列出）

#### 数据口径中仍为空的部分

| 数据 | 口径要求 | 当前 | 阻塞原因 |
|---|---|---|---|
| DEM 地形高程 | 覆盖外扩 bbox，30 m | `data/raw/dem/` 空 | 师弟采集中，预计 9/11 |
| 地物 / 土地覆盖 | 同 bbox | `data/raw/landcover/` 空 | 同上 |
| 道路 | 覆盖规划区域 | `data/raw/roads/` 空 | 同上 |
| 电台 / 天线真实型号库 | 短波 5–10、超短波 5–10 | `data/raw/equipment_docs/` 仍为示例行 | 同上 |
| 设备说明书与维修资料 | 20–50 份 | `data/raw/maintenance_docs/` 仅有模板 | 同上 |
| **故障知识图谱三元组** | **500–2000 条关系** | **`data/fault/knowledge_graph/` 空** | **未开工** |
| **故障规则** | **50–100 条规则** | **`data/fault/rules/` 空** | **未开工** |

后两项不依赖师弟的数据，是我方这条线的欠账，本体（实体类型、关系类型）已在
`docs/design/01_数据字典.md` 3.5 节定义，规则表达式语法已在 3.4 节定义，可以直接开工。

#### 其他未做

- 第 2 周及以后的算法模块（候选点筛选、部署规划、路由、频率、参数优化、多目标组网、干扰分析、故障诊断）全部未开工。
- 真实传播模型（ITU-R P.526 / P.1812 / P.533 / P.368）与真实 DEM 未接入，当前为占位实现。
  接口 `path_loss` / `link_budget` / `los_clearance` 已固定，替换时调用方不改。
- HTTP 接口层与异步任务框架未搭建，`docs/design/03_接口文档.md` 目前只是设计，无实现。
- `data/README.md` 注意事项要求「记录生成规则、随机种子」，目前只在脚本里，
  未单独成文（CLAUDE.md 中「待与 contraller 确认」的 `source` / `remark` 字段是同一件事）。

### 五、需要确认的事项

#### 1. 发现一处跨线的枚举口径冲突（**已于 2026-09-08 确认：沿用当前代码口径，见本文件最上一节**）

`data/dictionary/data_dictionary.md`（contraller 于 `d1f5aee` 提交）与
`docs/design/01_数据字典.md`、`scripts/gen_test_data.py` 的实际输出对不上：

| 概念 | contraller 的定义 | 当前代码与数据的实际口径 |
|---|---|---|
| 网络类型 | `network_type`：`HF` / **`VHF`** | `device_class`：`HF` / **`VUHF`** |
| 节点类型 | `node_type`：`fixed` / `mobile` / `relay` | `node_role`：`TASK` / `FIXED_STATION` / `RELAY` / `CANDIDATE`，另有 `mobility`：`FIXED` / `VEHICLE` / `MANPACK` |
| 状态 | `status`：`normal` / `degraded` / `fault` / `offline`（小写） | `status`：`NORMAL` / `FAULT` / `OFFLINE`（大写）；`degraded` 在本线中属于「受影响状态」`affect_state`，不是设备状态 |

按 CLAUDE.md「字段口径」一节，文档未涉及的字段以 `gen_test_data.py` 的实际输出为准，
但这份文件是 contraller 建的，**不跨线单方面改**。建议在群里确认后统一，
统一方向倾向于沿用当前代码口径（`VUHF` 比 `VHF` 准确，因为软需覆盖 30–400 MHz，
含 UHF 频段；节点角色与机动性是两个正交维度，合并成一个 `node_type` 会丢信息）。
确认后需同步三处：`gen_test_data.py`、`data/schemas/*_template.csv`、`docs/design/01_数据字典.md`。

#### 2. 待外包前端确认（见 `docs/design/03_接口文档.md` 第 5 节，共 7 项）

其中三项不早定会返工：

- **覆盖结果传输格式**：120×120 km @30 m 是 1600 万个栅格点，序列化成 JSON 约 300 MB，
  走不了接口。方案是分级等值面 GeoJSON + 带 bbox 的半透明 PNG 双通道，需确认前端能叠加图片图层。
- **坐标序**：`[lon, lat]`（GeoJSON 标准）还是 `[lat, lon]`。不确认必出错，且属于最难排查的那类错。
- **三路融合结果的展示位**：SR-5.2 要求可展开查看各路分项结论与置信度，前端需预留展开区域。

#### 3. 其他风险

- ML 路故障诊断依赖历史案例量，当前 240 条均为构造案例。
  软需 SR-5.2 d 已写明「未达启用门限时降级为规则 + 图谱两路」，
  建议按**两路融合 + 标注降级**作为第 5 周的主交付路径，ML 路作为加分项。
- `AGENTS.md` 风险提醒中「ML 层故障诊断是否纳入本次验收范围」尚未确认，与上一条相关。

### 六、下一步

按《项目安排》第 2 周（9/15–9/21）应交付「部署规划模块 + GIS 部署结果展示」。
DEM 未到之前可先做的（都跑在 `terrain.py` 上，换 DEM 不改代码）：

1. `SR-4.2.1.1` 候选部署位置筛选 —— 坡度、地物、交通可达性、站间距、通视约束
2. `SR-4.2.1.2` 移动电台数量与位置规划 —— 最少数量 / 指定数量两种模式
3. HTTP 接口层与异步任务框架
4. 故障规则库（50–100 条）与故障知识图谱三元组（500–2000 条）—— 本线欠账，不依赖外部数据

### 七、接手方式

```bash
git pull
python3 scripts/gen_test_data.py     # 重新生成全部测试数据
python3 scripts/validate_data.py     # 校验，退出码必须为 0
```

真实数据到位后的切换方式：

- 设备/天线型号库：放入 `data/raw/equipment_docs/`，重跑生成脚本自动从构造型号切到真实型号（示例行会被忽略）
- DEM：放入 `data/raw/dem/`，把 `scripts/terrain.py` 中的 `SyntheticTerrain` 换成 `DemTerrain`，其余代码不动
