# 工作交接

> 本文件记录**代码与数据线**（`scripts/` `data/` `docs/design/`）的工作进展。
> 每完成一段工作追加一节，最新的在最上面。
> 文档线（SRS 章节）的进展见 contraller 的记录。
>
> **下面这一节是常设的待办总表，每次交接前更新；再往下才是按时间倒序的进展记录。**

---

# 当前待办与待确认（截至 2026-09-22）

> **表中 A/B/C 编号出自 [`项目安排_需规对照版.md`](项目安排_需规对照版.md) 的「缺口登记表」章节**，
> 不是本文件的编号。那张表按**处理人**分三类：
>
> | 类 | 含义 | 处理人 |
> |---|---|---|
> | **A** | 需规缺项——《技术参考》或甲方指标有要求，**需规里没条文** | contraller（补需规） |
> | **B** | 排期缺项——需规**有**条文，但原《项目安排》没认领 | yoii1（排进周次） |
> | **C** | 需规编号缺陷 | contraller |
>
> B 类三项的共同点：条文都挂在 SR-1（GIS 监控中心）名下，看着像外包方的活，
> **但条文要求的计算部分在本线**，前端只负责渲染——所以原计划整个漏了。
>
> 两张表分工：**缺口登记表是全局视角**（三类都在，带需规条目与优先级），
> **本节是当前视角**（只列还没关掉的，按卡谁分组）。

## 一、卡在别人身上，本线做不了

| # | 事项 | 卡谁 | 影响 | 建议措辞 |
|---|---|---|---|---|
| 1 | **覆盖结果缓存的持久化方案** | 前端外包方 | 缓存现为**进程内、重启失效的演示级实现**。SR-1.1.2.6.2 c) 要求「覆盖数据读取规划模块的计算结果缓存，不独立发起实时全网计算」，只读入口已提供，但**存哪、存多久、何时失效**三件事必须两边一致，本线单方面定了也白定 | 「覆盖结果我们这边算完落缓存，前端按 key 只读。需要确认：落盘位置（文件/Redis/数据库）、保留时长、规划方案变更后如何失效」 |
| 2 | **需规补两条必补条文** | contraller | 见缺口登记表 A1、A2。**A1 通信编成层级是《技术参考》唯一标红项，需规里一个字都没有**——这两周围绕层级做的全部工作在需规里无处追溯；A2 「仿真规划时间 ≤5 分钟」是甲方验收指标，而 SR-4.2 g) 明文把算法耗时排除在性能指标外，**写反了** | A1 补入 SR-4.2.1.2 d)（d) 恰为空号）；A2 补 SR-4.2 h) 并修订 g) |
| 3 | **态势刷新 ≤1 分钟** | 前端外包方 | 甲方指标，需规无对应条目，且该能力归 GIS 前端 | 需向外包方明确 |
| 4 | **首次地图展示验证** | 前端外包方 | 第 2 周接口/联调栏要求「完成首次地图展示验证」。部署点、候选点、覆盖结果三个接口都已就绪，**联调本身需要对方配合** | 可约时间联调 |

## 二、待合作方澄清（不阻塞，已按假设建模并留痕）

| # | 事项 | 当前处理 | 若答案不同会怎样 |
|---|---|---|---|
| 5 | Ⅰ──Ⅰ 同行链路的用途 | 按「两个 Ⅰ 互为备份，到达任一即算到达」处理，根为集合而非单点 | 不改变求解结构 |
| 6 | Ⅱ固定站的装备数量（图中未标） | 容量标「待确认」，取满足编成连接数的工程默认值 HF=6 / VUHF=4，实现为可配置项 | 只需改 `echelon.ECHELON_CAP` 一处 |
| 7 | 链路质量四级门限 20/10/3 dB | 本项目自拟，已记入 `01_数据字典.md` 附录并注明依据 | 改一处，代码与图例随之同步 |

## 三、本线自己的待办

| # | 事项 | 说明 |
|---|---|---|
| 8 | **第 3 周正题**：SR-4.2.2.1 路由规划、SR-4.2.2.2 频率资源分配、SR-4.2.3 电台参数规划 | 按《项目安排》本周应做，尚未开始 |
| 9 | 缺口 A3 **预选配置 ≥3 种** | 甲方指标，归 SR-4.2.3，随第 3 周一起做 |
| 10 | 缺口 A7 衰落余量 6–12 dB | 设计参数，写进设计文档即可，不必补需规 |
| 11 | 分支定界的一般性 | 当前「已证最优」只意味着**达到了可证下界**，不是穷举了全部组合。下界不紧的场景仍可能次优，会如实报 gap |
| 14 | **传播模型仍是第 1 周占位实现** | `CLAUDE.md` 要求第 2 周换成真实 ITU-R P.526/P.1812/P.533/P.368。真实 DEM 已切换，真实传播模型未做。接口稳定，替换时调用方不改。在此之前所有 dB 数只能用于排序，不能作绝对量验收 |
| 13 | **与 @kyriezz0316 对齐 05 任务** | 他 2026-09-22 上传了一整套同任务实现，覆盖了本线 5 个文件（详见下方更正条）。需在群里说明两件事：① `scripts/` 与 `algorithm-service/` 按 CLAUDE.md 属本线目录，改动前先打招呼；② 把八问答复纪要（设计方案第 6 章）发他，层级与容量是硬约束 |
| 12 | 多方案「余量优先」区分度弱 | 最差余量 17 dB 未被新增电台改善——那条链路在既有基础设施里，新增电台碰不到。数据特性，非算法缺陷 |

## 已完成（本周内关闭的）

- ✅ 缺口 **B1 短波跳距与静区** —— `viewshed.hf_skip_geometry()`，10 MHz 实测静区 13–529 km
- ✅ 缺口 **B3 链路质量四级门限** —— 已记入数据字典附录，两条需规复用同一套判据
- ✅ 第 2 周欠项「对应的覆盖结果」—— `viewshed.py` + 三个覆盖路由
- ✅ 真实设备型号库切换 —— `设备型号来源: REAL`，16 型号 / 78 天线
- ✅ SR-4.2.1.2 全部十个步骤

---
## 2026-09-22 · 任务05 移动电台部署规划算法 交付（@kyriezz0316）

**本次交付**：05 算法实现 + HTTP 接口层 + 测试，纯标准库实现（无第三方依赖）。

**我放了哪些 / 放在哪了**：

| 仓库路径                     | 文件                                                                                                                                                                                   | 性质                                                 |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------- |
| `scripts/planning/`      | `feasibility.py` / `viewshed.py` / `deployment.py` / `metrics.py` / `solve.py` / `model.py` / `radio_db.py` + `test_smoke.py` / `test_deployment.py` / `test_integration.py`（共 10 个） | 新增                                                 |
| `algorithm-service/app/` | `server.py` / `deployment_service.py` / `test_http.py`                                                                                                                               | 新增                                                 |
| `algorithm-service/app/` | `data_loader.py`                                                                                                                                                                     | 覆盖（师兄原版基础上新增候选点降密度筛选，解决 VUHF 1137 候选求解超时；其余读取逻辑不变） |

师兄原有文件（`scripts/terrain.py`、`scripts/propagation.py`、`scripts/geo/*`、`algorithm-service/app/main.py` 等）未改动，05 直接复用。

**实测结果**：真实数据（156 节点 / 1137 候选 / 411 需求 / 太行山 DEM）下，HF ≈59s、VUHF ≈207s（均 < 5min 限制）；P2 输出 4/5/6 台三方案，需求满足率 100%；11 个测试全部通过。

**遗留待办（非阻塞）**：

1. 传播模型为占位实现（Q8），第二周接入真实 P.526/P.1812/P.533 后需重跑校准，电台数为临时值；
2. HTTP 层 `forced_include` / `forced_exclude` 未接（底层已预留）。

## 2026-09-22 · 实测：覆盖计算里的链路预算从未生效

被问到「覆盖都做完了，为什么说传播模型还是占位」，顺手实测了一遍，查出一个真问题。

**两者是不同层。** `viewshed.py` 是几何与口径层（径向视域、4/3 地球曲率修正、
区内裁剪、盲区聚合、短波环带）；`propagation.py` 是底下把「距离+地形+频率」
换算成 dB 的模型。覆盖调传播：`viewshed.py:109` 调 `vuhf_path_loss()`、
`:112` 调 `link_budget()`。覆盖做完 = 边界怎么画、面积怎么算做完了；
传播是占位 = 「多少 dB 才算够」这个数还没定标。

**实测结果：链路预算这道关形同虚设。** 五种台站覆盖格点中的最小余量为
22.3 / 26.1 / 30.1 / 24.3 / 27.3 dB，门限是 **6 dB**。把传播损耗人为加
5 / 10 / 15 dB，ND-0003 的覆盖面积一格不变（762.9 km²）。覆盖边界完全由
**地形遮挡 + 60 km 半径上限**决定。

**结论：** `vuhf_coverage()` 注释称「不是单纯的视域」，但在传播模型定标之前，
现在产出的结果与纯视域在数值上无法区分。占位模型基本是自由空间 + 单刃绕射 + 地物，
损耗估得偏低；真实 P.1812 带地形统计与时间/位置百分比，损耗会明显更大，
那时预算才会真正成为约束。

已作为 A5b 补入 [`docs/design/08_代码审查任务单.md`](docs/design/08_代码审查任务单.md)，
要求师弟独立复验并往下查是模型估低还是半径上限设小。

---
## 2026-09-22 · 安排两位师弟人工审查主线代码

产出 [`docs/design/08_代码审查任务单.md`](docs/design/08_代码审查任务单.md)：两人分工、22 条怀疑点、报告模板、
交叉复核规则、已知问题交底。报告交到 `docs/review/`。

分工按**可验证的缝**切，不按文件数切：甲查物理层与数据层（数字是不是真的），
乙查求解层与服务层（结论是不是真的）。要求每条结论都用**独立于被审代码**的方式复算，
「看了一遍逻辑没问题」记为未审查。

**顺带核出一条名实不符，须记在案：**
`scripts/propagation.py` 文件头自认「第 1 周占位实现」，全文 124 行，
实际是 自由空间 + 单刃绕射（P.526 近似）+ 地物损耗，短波地波在 P.368 四个锚点上拟合。
而 `CLAUDE.md` 明文要求**第 2 周**把占位实现换成真实 ITU-R 模型
（P.526 / P.1812 / P.533 / P.368）与真实 DEM。

- 真实 DEM：**已完成**（太行山 19.5 MB，2026-09-16 切换）
- 真实传播模型：**未完成**，仍是第 1 周的简化实现

影响：所有 dB 数（链路余量 17 dB、覆盖边界、链路质量四级判据）目前只能支撑
**相对比较与排序**，不足以作为绝对量对外验收。此前几条交接记录写「全链路复测达标」，
指的是流程与性能指标达标，不含传播模型精度——这一点当时没写清楚，在此补明。

已列为审查第一优先级（甲 A1），并补入待办。

---
## 2026-09-22 · 上一条交付的更正与归档位置（@yoii1）

上一条记录的「新放了哪些文件」与实际情况有出入，接口因此全线下线过一次。
事实、后果、处理如下，供后续追溯。

**事实：不是 10 新增 + 1 覆盖，是 9 新增 + 5 覆盖。**
`scripts/planning/` 下的 `feasibility.py` / `deployment.py` / `metrics.py` / `viewshed.py`
四个文件在上传前主线**已经存在**（提交 `a752408` 等），被整体替换：

| 文件 | 覆盖前 | 覆盖后 |
|---|---|---|
| `scripts/planning/feasibility.py` | 267 行 | 105 行 |
| `scripts/planning/deployment.py` | 688 行 | 509 行 |
| `scripts/planning/metrics.py` | 237 行 | 204 行 |
| `scripts/planning/viewshed.py` | 231 行 | 186 行 |
| `algorithm-service/app/data_loader.py` | 38 行 | 300 行 |

`data_loader.py` 也不是「其余读取逻辑不变」，而是整体重写：原有的
`candidate_sites()` / `nodes()` / `task_scenarios()` / `to_float()` 四个函数全部消失。

**后果（已验证）：**

1. `app/main.py` 导入即失败（`ImportError: cannot import name 'candidate_sites'`），
   连带 `app/candidate_sites.py`（第 1 周 SR-4.2.1.1 交付）一起坏，
   **五个接口全部下线**：候选点筛选、部署规划提交/查询、覆盖计算提交/查询/缓存读。
2. `scripts/planning/branch_bound.py`、`scale_test.py` 导入失败——
   两套代码的导入约定不同（包内相对导入 vs 平铺导入）。

**口径差异（这一条比文件冲突更要紧）：** 上传的这套求解模型是
顶点权重 Steiner 森林 + 集合覆盖，全套 grep 不到
`echelon` / `编成` / `node_subtype` / `II_NODE` / `III_MOBILE`，
`Site` 只有 `site_id / lon / lat / band / profile / kind`。
2026-09-22 拿到的八问答复中，2(b) Ⅱ–Ⅱ 无直连、2(c) 编成定额即容量约束、
3 按层级规划且同级可横向通联，在这套结构里没有落点——是数据结构层面的缺失，
不是调参能补的。

**处理：**

- 被覆盖的 5 个文件恢复到覆盖前状态（提交 `fab7303`），主线接口随之恢复；
- 他的整套 14 个文件**逐字节原样**搬到 [`alt_steiner/`](alt_steiner/)，
  目录层级与仓库根一致，附 [`alt_steiner/README.md`](alt_steiner/README.md)
  说明来历、差异、运行方式与可吸收的部分；
- 未使用 force push，历史未改写，他那边 pull 不会冲突。

**验证：** 主线 8 个 planning 模块 + 4 个服务模块全部导入通过
（`app/main.py` 只剩「FastAPI 未安装」这一条本机既有提示）；
备选实现在新位置挂 `PYTHONPATH=<仓库根>/scripts` 后 10 个模块同样全部导入通过；
`gen_test_data.py` 退出码 0，`validate_data.py` 0 ERROR 0 WARNING，数据无 diff。

**可吸收的部分**（与编成模型不冲突）：`model.py` 的 `RadioProfile` 参数归一
与挂高区间解析、`radio_db.py` 的按频段/型号取默认电台参数、
`data_loader.py` 的候选点按间距降密度筛选。

---
## 2026-09-22 · 补齐第 2 周欠项：覆盖计算与覆盖结果接口

负责人：yoii1

### 起因

核对第 2 周产出清单发现漏了一项：「**对应的覆盖结果**」。
合作方答复 5+6 去掉的是把覆盖率当**优化目标**，但覆盖范围本身仍要算——
它是 SR-1.1.2.6.2 态势呈现的依赖项，也是 SR-4.2.1 扩展流 1 盲区标注的前提。
我此前把它记成缺口 B1/B2 排到了 W3，**排错了，它是 W2 的交付项**。

### 新增 `scripts/planning/viewshed.py`（218 行）

SR-1.1.2.6.2 a) 要求两件不同的事，分开实现：

**超短波：径向视域（R3 变体）+ 链路预算 → 地形遮蔽后的不规则边界**
沿每条方位射线向外推进，维护射线上已出现的最大仰角，只有超过此前最大值才通视；
一次扫描 O(方位数 × 径向步数)，不必对每个格点单独做剖面。通视后再过链路预算——
需规要的是**通信**覆盖，不是单纯视域。含 4/3 地球等效曲率修正。
**不采用扇形/圆形近似**，这是需规明写的。

**短波：跳距与静区 → 环带形态**
单跳几何 `d_skip = 2h'·sqrt((f/foF2)² − 1)`；f ≤ foF2 时垂直入射可反射（NVIS），
不存在静区。与 `propagation.hf_nvis_loss` 用同一套 foF2（昼 7.5 / 夜 4.5 MHz）。

实测：

| 频率 | 结构 |
|---|---|
| 5 MHz | 0–300 km 连续覆盖，**无静区**（f ≤ foF2） |
| 10 MHz | 地波 0–13 km，**静区 13–529 km**，第一跳落区 529–847 km |
| 14 MHz | 跳距 946 km |

地形效应实测：山地站（1320 m）覆盖 422 km²，平原站（34 m）62 km²，**差 6.8 倍**。

### 新增 `algorithm-service/app/coverage.py`（205 行）+ 三个路由

- `POST /api/v1/plan/coverage` —— 提交计算，**命中缓存直接返回**
- `GET  /api/v1/plan/coverage/{task_id}` —— 查进度
- `GET  /api/v1/plan/coverage/cache/{cache_key}` —— **前端只读入口**

第三个路由是 SR-1.1.2.6.2 c)「覆盖数据读取规划模块的计算结果缓存，
不独立发起实时全网计算」的落点。缓存键按 (台站集合, 频段, 参数) 取哈希。

### 修了一处自相矛盾的统计

首次全网跑出「覆盖 92%、最大盲区 4672 km²」——14400 × 8% = 1156 km²，
和 4672 对不上。原因：台站在规划区边缘时覆盖会溢出区外，
溢出格子被计入了分子，而盲区只在区内找，**分子含区外、分母只有区内**。
裁剪到规划区内后：

```
规划区 14400 km²  已覆盖 9022 km² (63%)  未覆盖 5378 km²
盲区 10 块（≥5 km²）合计 5059 km²  —— 与未覆盖面积对得上
```

63% 才是真实数字。全网 104 个超短波台站耗时 10.8 s。

### 说明

返回体里的 `coverage_ratio` 明确标注「**仅作态势呈现与盲区标注之用，不是优化目标**」，
避免被误当成验收指标——部署求解的目标仍是全连通（合作方答复 5+6）。

缓存当前是进程内、重启失效的演示级实现。**持久化落盘位置与失效策略
需与前端外包方确认**，这是缺口登记表 B2 的剩余部分。

---

## 2026-09-22 · 切换到真实设备型号库

负责人：yoii1

### 起因

师弟已交付任务单 06 的产出（提交 `0dd9b52`，kyriezz0316，9/18）：
`device_model.csv` 16 个真实型号（HF 8 / VUHF 8）、`antenna_model.csv` 78 个天线。
此前所有实测数据都跑在构造参数上，需验证能否自动切换。

### 切换过程暴露三个问题

**1. 空字段导致生成脚本崩溃。**
任务单明确要求「手册里查不到 → 留空，不要编造」，真实库因此必然有空字段
（`bandwidth_khz` 缺 5 个、`rx_sensitivity_dbm` 缺 1 个、`power_consumption_w` 全缺）。
生成脚本直接 `float("")` 崩了。

新增 `scripts/devicespec.py`，三条可追溯的缺省规则：
同频段其他型号有值 → 取中位数（记「同类推定」）；全缺 → 取标准信道带宽
（短波 3 kHz SSB 话路、超短波 25 kHz FM 信道间隔，属通用制式常数非型号参数，
记「标准值代入」）；都不满足 → 不代入，让调用方报错。
每次代入都留痕，生成结束打印汇总。本次实测：1 个(型号,字段)缺口，影响 24 台设备实例。

**2. 频点池与真实型号的频段对不上 —— 由新增校验段 [9b] 抓出。**

```
[ERROR] 有 157 条链路的工作频率超出端点设备型号的频率范围
```

原因：频点池是我按**军用战术频段**构造的（超短波 30–88 MHz），
而师弟采集到的超短波电台全是 **136–870 MHz 的商用制式**（APX 8000、DP4000e、
PD985 等），两边没有交集。

改为**从型号库推导频段边界**（取各频段 fmin/fmax 的中位数），
池子永远与在库设备自洽；短波仍限制在 3–12 MHz，因为 NVIS 需低于昼间反射上限 7.5 MHz。

> 这条问题上周写校验段 [9] 时是「以防万一」加的，这次真的抓到了。
> 若没有这条检查，157 条物理上不存在的链路会静默进入数据集，
> 后续所有覆盖计算与部署方案都建立在假链路上。

**3. `default_freq` 只检查了链路一端的频率范围。**
两端型号频段不同时会选出一端够不着的频点。改为取两端range 的交集；
无交集则该链路不成立。

### 切换后的数据变化

| 项 | 构造参数 | 真实型号 |
|---|---|---|
| 设备型号来源 | CONSTRUCTED | **REAL**（16 型号 / 78 天线） |
| 链路状态 EXCELLENT | 290 | 250 |
| 链路状态 WARNING | 45 | 80 |
| 通联需求 | 418 条 | 394 条 |
| 部署方案最差链路余量 | 27 dB | **17 dB** |

真实设备比构造参数保守，余量分布更贴近实际，这是好事。

### 全链路复测（真实参数）

```
满负荷规划       21.7 s   [指标 ≤300 s]  达标 ✓
20% 失效重规划   0.10 s   [指标 ≤180 s]  达标 ✓
方案1/2/3 均 100% 连通，下界 5 部实得 5 部
gen + validate   0 ERROR 0 WARNING
```

### 没做的

**覆盖计算（`viewshed.py`）仍未实现**，这是第 2 周产出清单里「对应的覆盖结果」
一项，以及 SR-1.1.2.6.2 的态势呈现依赖项。下一步就做这个。

---

## 2026-09-22 · 步骤 8–10 完成，SR-4.2.1.2 全流程跑通

负责人：yoii1

### 步骤 8 增量重规划（参考项）

`deployment.py` 新增 `affected_by()` / `replan_incremental()`。
层级结构在这里是优势——一个节点失效只波及它的子树，不扩散到全网。
正常工况仍无解时切换 damaged 矩阵重试，并把由此产生的链路显式标注为
「跨层级降级链路」（标红段「必要时可以跨层级通信（存在作战损伤时）」的落点）。

### 步骤 9 HTTP 接口层

新增 `algorithm-service/app/deployment.py`，注册两个路由：

- `POST /api/v1/plan/deployment` —— 立即返回 `task_id`
- `GET /api/v1/plan/deployment/{task_id}` —— 查询 stage / progress / result

需规 SR-4.2 g) 要求异步任务模式，用**标准库 threading** 实现，不引第三方任务队列。
算法核心仍全部在 `scripts/planning/` 下，纯标准库；本层只做参数校验、任务编排、序列化。

响应中每个部署点给出 `echelon_role`、`serves_node_ids`、`port_usage`（各频段占用/容量）；
无可行解时返回 `code=3001` 与按最小割分类的 `suggestions`。
**`metrics` 里没有 coverage_ratio**（合作方答复 5+6）。

### 步骤 10 规模测试

新增 `scripts/planning/scale_test.py`。按软需 SR-4.2 f 上限口径实测：

规模：节点 132（持短波 76 / 持超短波 104）、设备 414 台、
候选位置 1100 × 4 类型 = 4400 个候选、通联需求 418 条。

| 阶段 | 实测 |
|---|---:|
| 加载数据与地形 | 0.1 s |
| P1 全量求解（建矩阵 + 惰性贪心 + 分支定界） | 4.8 s |
| 多方案生成（3 套） | 16.4 s |
| 方案评估 + 418 条需求路径核算 × 3 | 0.0 s |
| **合计（满负荷规划）** | **21.3 s**　指标 ≤300 s，**余量 14 倍** ✓ |
| 20% 节点失效增量重规划（3 组种子最慢） | **0.10 s**　指标 ≤180 s ✓ |

方案 05 §4.12 已用实测数替换原估算，并记录了朴素做法 1306 s 超标 4 倍的对照。

### 全部检查

- `gen_test_data.py` + `gen_algorithm_design_data.py` + `validate_data.py` → **0 ERROR 0 WARNING**
- `echelon.py` 自检通过
- `flow.py` 反例自检：b-匹配口径可行、单位流口径矛盾（守住那个建模错误）
- `tests/test_candidate_sites.py` 3 项通过

### SR-4.2.1.2 需规条目覆盖

| 条目 | 状态 |
|---|---|
| a) 基于覆盖最优算法选点 | ✅ 分层指派 + 惰性贪心 |
| b) 自动计算最少电台数 | ✅ P1，含三个下界与分支定界 |
| c) 人工指定数量求最优位置 | ✅ `p2_solve`，p<p_min 时给失联清单 |
| 扩展流 1 无可行解时提示原因 | ✅ 最小割四分类诊断 |
| 扩展流 2 多组备选方案对比 | ✅ 3 套方案 + 统一指标 |

### 进度说明

今天 **2026-09-22，已进入第 3 周**。SR-4.2.1.2 的 10 个步骤全部完成，
比方案 §7.3 的「本周 0–4、顺延第 3 周 5–10」提前——实际用了约 1.5 天而非 8 天，
主要因为惰性求值省掉了大量求解、且测试数据本身编成结构规整。

按《项目安排》，第 3 周任务是 SR-4.2.2.1 路由规划、SR-4.2.2.2 频率资源分配、
SR-4.2.3 电台参数规划，**可按原计划开始**。

另：`项目安排_需规对照版.md` 缺口登记表中排在 W3 的三项仍未做——
A3 预选配置 ≥3 种、A7 衰落余量、**B1 短波跳距与静区计算**。B1 是需规有条文
（SR-1.1.2.6.2 a）但原计划未认领的，需要排进本周。

### 仍待确认（不阻塞）

1. Ⅰ──Ⅰ 同行链路用途（当前按「互为备份，到达任一即算到达」处理）
2. Ⅱ固定站装备数量（图中未标，容量按待确认取工程默认值）
3. 需规两项必补条文（A1 通信层级、A2 ≤5 分钟）仍需 contraller 处理

---

## 2026-09-18 · 步骤 6–7 完成：P2 指定数量、多方案、方案评估

负责人：yoii1

### 新增 `planning/metrics.py`（238 行）+ `deployment.py` 扩充

**通联需求按树上路径核算。** 合作方答复 3：411 条需求保留，
**同一上级的下级可以横向通联，不需要先上行再下行**。
因此每条需求算最近公共祖先（LCA）路径：同父兄弟 2 跳、Ⅲ 之间直连 1 跳。

基线实测（132 台站）：**418 条需求全部可达**，跳数 1–9 跳，
**150 条（36%）走横向捷径未绕经 Ⅰ** —— 合作方那条答复在数据上生效了。

### 过程中修的两个错

**1. 133 条需求算不出路径，但所有节点都显示连通。**
原因：求解器不给根指派上级，而 Ⅰ固定站 与 Ⅰ机动站 各自成树
（祖先链顶端 76 个在 Ⅰ固定站、56 个在 Ⅰ机动站）。
图中这两者之间**有一条同行链路**（互为备份），跨树路径应从这一跳走。补上后 418 条全可达。

**2. 多方案里加了电台，连通率反而从 100% 掉到 86%。**
原因：`p2_solve` 把「连通增量」与「冗余增量」放进同一个排序键比较，
而两者量纲不同——「能多给 3 个节点做备份」压过了「能新连通 2 个节点」。
改为**连通增量优先，冗余增量只在没有任何连通增量时才启用**。

### 一处如实修正：平均跳数不是可调目标

原计划方案 2 为「时延优先，偏向缩短平均跳数」。实测加 1 部电台后
平均跳数仍是 4.99，与最经济方案**完全相同**——因为跳数由编成深度决定
（Ⅳ→Ⅲ→Ⅱ→Ⅱ固定站→Ⅰ 最少就是 4 跳），加电台改不了它。

方案 2 改为**「余量优先」**：抬高最差链路的余量，这是加电台真能改善的。

### 多方案实测（SR-4.2.1.2 扩展流 2）

| 方案 | 电台 | 连通率 | 最差余量 | 余量中位 | 有备份上级 |
|---|---|---|---|---|---|
| 方案1 最经济 | 5 | 100% | 27 dB | 61 dB | 105/121 |
| 方案2 余量优先 | 6 | 100% | 27 dB | 61 dB | 106/122 |
| 方案3 抗毁优先 | 7 | 100% | 26 dB | 59 dB | **114/123** |

生成耗时 16.8 s。

**方案 2 的区分度很弱**（最差余量 27 dB 没动）：最差的那条链路在既有基础设施里，
新增电台碰不到它。这是测试数据本身链路质量偏好造成的，不是算法问题，如实记录。
方案 3 的抗毁性改善是真实的（有备份上级的节点 105 → 114）。

### SR-4.2.1.2 c) 人工指定数量

给 3 部（少于最少需求 5 部）：连通 116/121，仍失联 5 个，
输出失联清单供提示放宽参数——即扩展流 1 要求的「提示原因并给出参数调整建议」。

### 新增评估指标

连通率、新增电台数与类型构成、下界与 gap、建链数、链路余量（最小/中位）、
端口占用满载数、**有备份上级的节点数**（抗毁性直接度量）、
通联需求跳数分布与横向捷径占比、单点失效影响面 Top。

**覆盖率不在指标内**（合作方答复 5+6）。

---

## 2026-09-18 · 步骤 5 完成：自研分支定界与三个下界

负责人：yoii1

### 新增 `planning/branch_bound.py`（172 行）

合作方答复问题 1「不可以用求解器，我们自己写求解」，本模块是外层选址的收敛手段。

**三个下界，取最大者：**

| 下界 | 依据 | 本场景取值 |
|---|---|---|
| LB1 容量 | 某层 n 个节点待接入、单个上级能带 c 个 → 至少 ⌈n/c⌉ 个上级 | 5 |
| LB2 互斥 | 若两个失联节点没有任何共同的候选服务者，必须各配一部电台；求这种两两互斥集合的规模 | 1 |
| LB3 端口 | 失联节点数 ÷ 单部电台最大下行端口数，向上取整 | 5 |

LB2 的最大互斥集合是独立集问题（NP 难），用贪心求「下界的下界」——仍是合法下界。

**分支定界**：每步取增量上界最高的候选，分「选」与「不选」两支；
不选支把该候选移出可用集合，避免重复搜索同一组合。
每层只展开增量最高的三个分支，带时间上限，超时则返回当前最优并如实标注。

### 验证

贪心解在测试场景已达下界，分支定界不会被触发。为验证搜索本身正确，
**故意喂给它一个放大的上界 7 部**：

```
分支定界改进：5 部
结果 5 部，搜索节点 16，剪枝 8 次，0.2 s，搜索穷尽
验证：失联 0 个 → 可行 ✓
达到下界 5 → 已证最优 ✓
```

### 接入 P1

`p1_solve` 新增 `refine=True`：贪心已达下界则跳过搜索并标注「已证最优」；
未达下界则进入分支定界尝试改进。全量规模实测：

```
下界 5 部（容量 5 / 互斥 1 / 端口 5），实得 5 部，gap 0%（已证最优）
总耗时 5.0 s
```

### 说明

「已证最优」的含义是**达到了可证下界**，不是穷举了全部组合。
下界不紧的场景（LB 远小于真实最优）仍可能给出次优解并如实报告 gap。

---

## 2026-09-18 · 规模测试暴露超标，用惰性求值解决

负责人：yoii1

### 起因

步骤 1–4 的实测只跑了 120 个候选位置。按软需全量规模（1100 候选位置 × 4 种可部署类型
= 4400 个候选）实测，**远超 5 分钟指标**：

| 项 | 朴素做法 |
|---|---|
| 台站总数 | 132 + 4400 = 4532 |
| 建可行性矩阵 | 4.7 M 对，**49.8 s** |
| 单次分层指派 | 59.3 ms |
| 贪心每轮 | 4400 × 59.3 ms = **261 s** |
| 5 轮合计 | **1306 s**（指标 ≤300 s） |

### 两处优化

**1. 建矩阵时跳过候选×候选**（`FeasibilityMatrix(candidate_pairs=False)`）

4.7 M 对里绝大多数是候选之间的，而贪心每轮只选中一两个候选，这些对算了也白算。
跳过后只算候选×现有。选中之后再用新增的 `extend_pairs()` 补算那几对，
代价可忽略。矩阵规模 4.7 M → 26 万对。

**2. 贪心改为惰性求值**

每轮先用位运算算出每个候选的增量**上界**（微秒级）：
`上界 = 它能够到的失联节点数，再被自己的端口容量截断`（自己还要留一个上行端口）。
按上界降序排，只对前 `shortlist=24` 个做精确求解，且一旦「剩余上界 ≤ 当前最优增量」
即可剪枝停止——因为已按上界降序，后面的不可能更好。

排序键为 `(增量上界降序, 部署代价升序, 平均链路余量降序)`。第三项是实质性择优依据；
最初没有它，并列时只能按下标决定，选出来的点全挤在候选表前五行、与地理分布无关。

### 结果

| 项 | 优化前 | 优化后 |
|---|---|---|
| 精确求解次数 | 22000 | **5** |
| 总耗时 | 1306 s（推算） | **4.9 s** |
| 解质量 | — | 容量下界 5 部，实得 5 部，**gap 0%** |

5 个新增 Ⅲ 机动站分布于 114.06–115.15E、37.09–37.89N，地理上分散。

### 说明

gap 0% 只对本测试场景成立（14 个 Ⅳ 失联、容量下界恰好可达），
**不构成一般情形的最优性保证**。分支定界（步骤 5）尚未实现。

---

## 2026-09-18 · 步骤 1–4 完成：编成模型、可行性矩阵、自研求解、P1 部署

负责人：yoii1

### 新增四个模块（均为纯标准库）

| 模块 | 行数 | 职责 |
|---|---|---|
| `planning/echelon.py` | 246 | 编成口径的**唯一定义处**：五行层级、25 条允许关系、容量表、自检 |
| `planning/feasibility.py` | 205 | 两张位图矩阵 × 两频段（正常 / 损伤工况） |
| `planning/flow.py` | 168 | 自研最小费用最大流（SSP + SPFA）+ 带容量的上级指派 |
| `planning/deployment.py` | 330 | 分层指派、无解诊断、P1 外层贪心、容量下界 |

`gen_test_data.py` 改为从 `echelon.py` 引用编成常量，不再另存一份。
改后重跑，数据逐字节不变（种子可复现）。

### 编码阶段证伪了方案 §4.3 的流模型

方案第三版写「整个上行问题 = 单源单汇有容量网络流，**最大流饱和 ⟺ 全连通**」。
动手前构造三节点算例检验，**该说法不成立**：

```
R(容量1) ── A(容量2) ── B(容量1)      可行链路：A─R、B─A
```

- 按端口口径：R 用 1/1、A 用 2/2、B 用 1/1，全不超容量 → **可行**
- 按单位流口径：A、B 各一个单位都要过边 A→R（容量 1），最大流 1 < 2 → **不可行**

根源：合作方说的容量是**装备台数即端口数（度数）**，而单位流把「经过某节点的流量」
当成端口占用。中继节点转发时，一条链路可承载它整个子树的流量，却只占一个端口。

**改用正确模型：带度数约束的支撑树。** 按层自底向上，每层的「谁挂谁」是
带容量的二部图匹配（b-匹配）——b-匹配正是最大流的标准形式，
流在这里是用对地方的：它匹配的是「下级 → 上级名额」，不是「流量 → 汇点」。

该反例已固化为 `flow.py` 的自检用例，方案 §3.7 / §4.3 已改写并留勘误说明。

### 另修正一处端口记账错误

第 1 层跑 Ⅳ→Ⅲ 时 Ⅲ 的 4 个 cdb 口全空，贪心把 4 个 Ⅳ 都挂上去，
等第 2 层轮到 Ⅲ 自己上行时已无端口可用——而代码只检查上级余量、不检查下级余量，
**错误地判为配上了**。由「撤走一半 Ⅲ」的算例暴露。

改为**每个非根节点建链前先预留一个上行端口**。预留之后每个 Ⅲ 的 cdb 端口
是 1 上行 + 3 下行，与编成图「双上级 + 2 个 Ⅳ」的画法一致。

### 实测结果

**基线**（132 台站，未新增部署）：全连通，分层指派 0.02 s，建矩阵 0.14 s。

| 频段 | 持有台站 | 物理可行 | 编成允许 | 编成剪掉 |
|---|---|---|---|---|
| HF | 76 | 2622 条 | 1715 条 | 35% |
| VUHF | 104 | 3968 条 | 1472 条 | 63% |

**容量压力测试**（撤走 14/28 个 Ⅲ 机动站）：
14 个剩余 Ⅲ × 3 个空闲 cdb 口 = 42 个名额，56 个 Ⅳ 中 **14 个失联**——数字精确对上。
诊断正确输出「VUHF 上级端口已用尽（N 个在役合法上级全满，类型 III_MOBILE）」，
对应处置正是增设 Ⅲ 机动站。

**P1 部署求解**（同场景，120 个候选位置）：

```
容量下界 5 部，实得 5 部，gap 0%  （达到下界，本场景最优）
新增 5 个 Ⅲ 机动站，全部 123 个台站连通
总耗时 6.4 s
```

### 与方案的偏差

1. §4.3 由「按频段拆点建流网络」改为「分层指派」，理由见上。
   拆点建图的思路仍保留在频段分离上——可行性矩阵按频段分开建，
   跨频段点对在任何一张矩阵里都不存在，「db 不能和 cdb 相连」由数据结构保证。
2. 分支定界（步骤 5）尚未实现，当前 P1 是贪心 + 冗余消除，以容量下界度量质量。
   实测场景已达下界，但不保证一般情形最优。

### 没做的（下一步：步骤 5–10）

5. `branch_bound.py` 自研分支定界 + 另两个下界（最小割下界、LP 松弛）
6. P2 指定数量求解 + 多方案生成
7. `metrics.py` 方案评估 + 411 条需求路径核算
8. 增量重规划（参考项）
9. HTTP 接口层
10. 规模测试与文档

另：`p1_solve` 目前对候选点做了外部稀释（测试取 120 个），
全量 1100 个候选 × 4 种类型的规模测试尚未做，耗时待实测。

---

## 2026-09-18 · 步骤 0 完成：数据结构改为编成模型

负责人：yoii1

### 做了什么

方案 7.1 的前置数据改动已完成。改动前实测：156 个节点对应 156 台设备，
**每节点恰好 1 台单频设备，双频节点 0 个**——一台设备一次维持一条链路，
故每节点容量都是 1，一条链路进来就占满，**中继在原数据上物理上不可能发生**。

**1. `node` 表新增四个字段**

| 字段 | 取值 | 作用 |
|---|---|---|
| `echelon` | `I`/`II`/`III`/`IV` | 层级约束的根 |
| `node_subtype` | 10 类（见数据字典 0.6） | Ⅱ 层内部有主从，只看 echelon 分不出 Ⅱ固定站与Ⅱ机动站 |
| `parent_node_id` | 多归属以 `;` 分隔 | 上行链与树上路径判定 |
| `relay_capable` | `true`/`false` | 按编成规则推导，真实装备待 06 任务单核实 |

`device_class` 由单值改为**多值**（`HF;VUHF`）。

**2. `device` 表按编成定额生成多台**，容量 = 该 `(node_id, 频段)` 的行数，不新增容量字段。

**3. 结果**

| | 改动前 | 改动后 |
|---|---|---|
| 节点 | 156 | 132 |
| 设备 | 156（每节点 1 台） | **414**（短波 174 / 超短波 240） |
| 双频节点 | 0 | **48** |
| 持短波 / 持超短波 | 52 / 104 | **76 / 104**（软需 ≥50 / ≥100 均满足） |

编成构成：Ⅰ固定 1、Ⅰ机动 1、Ⅱ固定 4、Ⅱ机动 8、Ⅱ节点 8、**Ⅲ 28、Ⅳ 56**、a车 2、b车 12、背负式 12。

> Ⅲ 请求 30 实际 28、Ⅳ 请求 60 实际 56，是**上级端口容量用满**所致，非缺陷——
> Ⅱ 行3 的 cdb 端口共 8×2+8×5=56 个，28 个 Ⅲ 各占 2 个（双上级）正好用尽。
> 生成脚本会把这个差额打印出来。这正是容量约束在起作用。

**4. `device_models` 表头未动**，师弟的 `06_设备参数补全任务单.md` 不受影响。

### 顺带修掉的三个静默错误

改成多值后，原先靠「比较两个节点的 `device_class` 字符串」来保证同频段的地方全部失效。
这类错误**不报错，只是数据悄悄少了东西**：

| 位置 | 原写法的问题 |
|---|---|
| `gen_test_data.py` 链路枚举 | Ⅳ(`VUHF`) vs Ⅲ(`HF;VUHF`) 字符串不等 → **Ⅲ–Ⅳ 链路会被整个跳过** |
| `gen_test_data.py` 骨架生成树 | 并查集跨频段合并，db 网和 cdb 网会被错误连通 |
| **`validate_data.py` 连通性检查** | 按字符串筛频段，**把双频的 Ⅱ/Ⅲ 骨干全排除**，导致误报 16 个连通分量、100 条必要需求不连通 |

统一改为按 **(节点, 频段)** 二元组迭代——这与方案 4.3 流模型的拆点结构是同一套表达，
生成器与求解器口径一致。

另修 `gen_algorithm_design_data.py` 的 `shortest_route`：堆元素代价相同时
Python 会去比较后面的 dict 而抛 `TypeError`，加序号做 tiebreaker。

### 新增校验段 [9] 频段一致性

原先校验器**没有任何一条检查频段**，全靠生成器自觉——因为单频时违规在结构上就发生不了。
改成多频后这层天然保护消失，补四条：

1. 链路两端的**设备**频段必须与链路频段一致（查 `device`→`device_model`，不查字符串）
2. 链路工作频率必须落在两端设备型号的 `[freq_min, freq_max]` 内
3. 通联需求两端必须共享至少一个频段
4. 各 `(节点,频段)` 的链路占用与容量对比（信息项，可达图超容量属正常）

### 结果

- `python3 scripts/gen_test_data.py` + `gen_algorithm_design_data.py` + `validate_data.py`
  → **0 ERROR，0 WARNING**
- `tests/test_candidate_sites.py` 3 项全通过（本机无 pytest，手动逐个调用）
- 三处定义已同步：生成脚本表头、`data/schemas/nodes_template.csv`、`docs/design/01_数据字典.md`

### 方案文档同步修正

**Ⅱ──Ⅲ 走 cdb，不是 db。** 由 Visio 连线度数反推确定，不再是假设：
Ⅲ 图上度数为 4，恰等于其 cdb 台数 → 4 条连线全是 cdb（2 上行 2 下行）；
Ⅱ机动站度数 3 = db×1 + cdb×2，cdb 下行两个 Ⅲ、db 上行 Ⅱ固定站。
**Ⅲ 的 db×1 在左支未被占用，正是标红段「与Ⅱ、Ⅲ的短波电台连接」的右支接入口。**
方案 6.9 第 1 条待确认项已关闭。

### 没做的（下一步：步骤 1–4）

1. `planning/echelon.py` 五行编成模型与跃迁规则
2. `planning/feasibility.py` 两张可行性矩阵 × 两频段
3. `planning/flow.py` 最小费用最大流 + 按频段拆点建图
4. `planning/deployment.py` P1 求解与最小割诊断

仍待确认（不阻塞）：Ⅰ──Ⅰ 同行链路用途、Ⅱ固定站装备数量（当前按待确认取工程默认值）。

---

## 2026-09-18 · 据八条答复重写部署规划方案（第三版，已定稿）

负责人：yoii1

### 起因

第二版第 6 章提出的八个待确认问题，合作方已全部答复。其中三条**改的是模型本身**，
不是补细节，因此第 3、4 章整章重写。

### 八条答复与落点

| # | 问题 | 答复 | 落点 |
|---|---|---|---|
| 1 | 求解器取舍 | **不可以用求解器，自己写求解** | 全自研：最小费用最大流（约150行）+ 分支定界（约200行） |
| 2a | Ⅳ 能否降级挂 Ⅱ | **需要增设 Ⅲ** | Ⅳ 上级严格唯一为 Ⅲ；Ⅲ机动站成为唯一接入手段 |
| 2b | 同级链路能否中继 | **Ⅱ–Ⅱ 没有，Ⅲ–Ⅲ 可以** | Ⅱ机动站与Ⅱ机动节点站不直连，经 Ⅱ固定站转；Ⅲ 之间可直连并转发 |
| 2c | 编成定额还是容量 | **是定额，当成容量约束** | 无容量设施选址 → **有容量**；贪心 1−1/e 保证失效 |
| 3 | 411 条需求怎么办 | **保留，与层级结合；同一上级的下级可横向通联** | 化归为「所有节点连通到根」；横向通联作路径优化项 |
| 4 | Ⅳ 是否都要双上级 | **不需要** | Ⅳ 层单上级可接受，抗毁性移交 SR-4.2.4 |
| 5+6 | 覆盖率怎么算、怎么权衡 | **覆盖率不计算，以全连通为唯一目标** | **改动最大的一条**，见下 |
| 7 | 与 SR-4.2.4 边界 | **本算法保证全联通，多目标优化留后面** | 只负责可行性；原 4.9 整节移交 |
| 8 | P.1546 还是 P.1812 | **以你的理解选择** | 链路判据用 P.1812，覆盖呈现用 P.1546，ITM 不做 |

### 三处模型级改动

**1. 覆盖率退出优化**（答复 5+6）——原 3.5「覆盖率判据」整节删除；
P1 去掉约束 C(X)≥θ，P2 去掉目标项 β·C(X)；原 4.3 覆盖位图从主流程移出，
改为独立异步模块只服务 SR-1.1.2.6.2 态势呈现。
**计算时间预算由 75–110 s 降至 35–60 s**（覆盖位图原占 40–60 s）。

**2. 容量约束**（答复 2c）——`cap(v,b)` = 该节点该频段设备台数。
上一版建模为无容量多级设施选址，卖点是「每层子模、贪心有 1−1/e 保证」，
**加容量后该保证失效**。改为「外层选址（自研搜索）+ 内层分配（有容量网络流，精确解）」。

关键观察：给定已开设电台集合，「所有节点能否连通到根」是标准有容量最大流可行性问题，
**可精确求解，不需要启发式**。按频段拆点建图，拆点边容量 = 设备台数，
双频节点加内部转接边——这恰好表达「db 不能和 cdb 相连，转换只能在节点内部」，
且 Ⅲ–Ⅲ 中继被流模型自然吸收，无需为层内多跳写专门逻辑。

**3. 五行层级**（答复 2b + 复核 Visio）——按纵坐标重核，图是**五行不是四行**：
Ⅱ固定站 y=3.68 单独占一行，是 Ⅱ机动站/Ⅱ机动节点站（y=2.57）的上级。
上行链实际是五跳：`Ⅳ → Ⅲ → Ⅱ机动站/节点站 → Ⅱ固定站 → Ⅰ`。

### 过程中的两处更正

1. **误判 Visio 与答复矛盾。** 曾把「Ⅱ固定站──Ⅱ机动站」当作同级链路，
   据此认为图上有 Ⅱ–Ⅱ 线、与答复「Ⅱ–Ⅱ 没有」冲突。按行高重核后确认那是**父子**关系，
   同行的两个 Ⅱ 之间确实无连线，图与答复一致。
2. **Ⅲ 的容量不是 2 是 4。** 图中每个 Ⅲ 只画 2 个 Ⅳ，但框内标注 cdb 设备 ×4，
   画出来的 2 个是示例而非上限。

另核实：图中 Ⅲ 行上方与正中的长横线是多归属造成的**交叉走线**（连线 42、43），
不是 Ⅲ──Ⅲ 链路。碰到 Ⅲ 的 8 条连线，另一端全部是 Ⅱ 或 Ⅳ。

### 结果

- `docs/design/05_移动电台部署规划算法方案.md` 860 → 1131 行，状态改为**已定稿，可开工**
- 第 6 章由「需要请教的问题」改为「八个问题的答复记录与落点」，逐条对应
- 工期由 5.5 天重估为 **8 天**（容量+流 +1.5、树上路径 +0.5、Ⅲ层中继 +0.5、
  分支定界 +1；覆盖位图 −0.5、抗毁性移交 −0.5）
- `python3 scripts/validate_data.py` 退出码 0，0 ERROR 0 WARNING

### 没做的（下一步）

**7.1 的数据结构改动尚未执行，这是开工第一件事。**
实测现有数据：156 个节点对应 156 台设备，**每节点恰好 1 台单频设备，双频节点 0 个**。
一台设备一次维持一条链路，故当前每节点容量都是 1——一条链路进来就占满，
**中继在现有数据上物理上不可能发生**。而编成要求 Ⅱ机动站 3 台、Ⅱ机动节点站 6 台、Ⅲ机动站 5 台。

需要改：

- `nodes` 加 `echelon` / `node_subtype` / `parent_node_id` / `relay_capable` 四个字段
- `device_class` 由单值改多值（`HF;VUHF`）
- `devices` 按编成定额生成多台（156 → 四百余行）
- 同步三处：`gen_test_data.py` 表头、`data/schemas/*_template.csv`、`01_数据字典.md`

**`device_models` 表头不改**——战术电台单信道，一台一条链路，无须 `max_concurrent_links`。
这也避开与已下发的 `06_设备参数补全任务单.md` 冲突（该任务单要求不得改表头）。

### 待确认（均不阻塞，已按假设建模并标注）

1. **Ⅲ 上行走 db 还是 cdb？** 若走 cdb 则 Ⅲ 下行名额由 4 减为 3，直接改变电台数。
   当前按 db 上行建模，实现为可配置项。
2. **Ⅰ──Ⅰ 同行链路用途**合作方未答。按「两个 Ⅰ 互为备份，到达任一即算到达」处理。
3. **Ⅱ固定站装备数量**图中未标注。容量标「待确认」，默认不设限。

### 分期建议

第 2 周剩余不足 8 天，建议切两段：本周做步骤 0–4（4.5 天，交 P1 全连通求解，
覆盖 SR-4.2.1.2 a) b) 与第 2 周要求的五项输出），步骤 5–10 顺延第 3 周（3.5 天）。

---

## 2026-09-17 · 缺口编入工作安排

负责人：yoii1

### 起因

上一节建立对照关系后，进一步核查「工作是否全覆盖」。
SR-4.2 / SR-5 / SR-6 下所有子条目六周均有认领，但发现**三处算法侧义务藏在 SR-1 前端条目中**——
条文挂在 GIS 监控中心（已外包）名下，容易被当成外包方的活，实际计算部分在本线，
原《项目安排》未认领。

### 新查出的三项（B 类：需规有条文、计划未认领）

| # | 内容 | 需规条目 | 现状 | 已排入 |
|---|---|---|---|---|
| B1 | 短波跳距与静区计算 | SR-1.1.2.6.2 a) | `propagation.py` 仅有地波（P.368 已标定）与 NVIS，**NVIS 按定义无静区**；文件头已写明第 2 周替换但未实现 | W3 |
| B2 | 覆盖结果缓存 | SR-1.1.2.6.2 c) | 接口文档 §2.5 已设计 contour + raster_png 双通道，但缓存本身（存哪、存多久、何时失效）无约定 | W2 定约定 → W3 落地 |
| B3 | 链路质量四级门限 | SR-1.2.2.5.6.2 b)、SR-6.2 | `margin_to_state` 输出 EXCELLENT/GOOD/WARNING/FAULT，**级数与需规四级一致**；但门限 20/10/3 dB 为自拟，未记入数据字典 | W2 锁定 → W6 复用 |

B1 需注意尺度：规划区 120×120 km，NVIS 覆盖 0–300 km 且无静区，
低仰角天波第一跳落区 300–800 km——**静区落在规划区之外**。
该项服务 SR-1 全域态势呈现，须按全域尺度单独算（P.533 单跳几何 + MUF/LUF），
不能复用部署规划的 120 km 覆盖位图。

### 做了什么

**1. 缺口登记表重构为三类**

原来是一张平铺的 8 项表。问题在于两种缺口性质不同、处理人也不同，混在一起看不出该找谁：

- **A 类 需规缺项**（8 项）—— 需规无条文，处理方式是补需规，归 contraller。
  **须补的只有 A1 通信编成层级、A2 仿真规划时间 ≤5 分钟**；
  A3 建议补，A4 可补可不补，A5 属外包方，A6–A8 写进设计文档即可。
- **B 类 排期缺项**（3 项）—— 需规有条文，处理方式是排入周次，归 yoii1。
- **C 类 编号缺陷**（1 项）—— SR-4.2.1.1 / SR-4.2.1.2 的 d) 为空号。

每项补「涉及周次」列，并新增「缺口处置排期一览」表，按周列出该周需处置的缺口编号。

**2. 三项 B 类缺口编入对应周次正文**

第 2、3 周各新增一节「▎本周承担的前端条目算法侧义务」，
列明需规条目、原文要求、本线义务、归属；新增记号 🔶 代前端算。
第 2、3、6 周的「▎缺口提示」相应补充条目。

**3. 第 0 节补缺口索引**，说明三类缺口的性质与去向。

### 结果

- `项目安排_需规对照版.md` 517 → 597 行
- 缺口总数 8 → 12（A 类 8、B 类 3、C 类 1）
- `python3 scripts/validate_data.py` 退出码 0，0 ERROR 0 WARNING

### 没做的

1. **B1 短波跳距与静区未实现**，只完成排期与实现要点说明，实现在第 3 周。
2. **B2 缓存约定未定**，需与前端外包方确认存储位置、粒度、失效策略。
3. **B3 门限未记入数据字典**，待第 2 周锁定后同步 `01_数据字典.md`。
4. 需规本身未改，A1/A2 两项条文需 contraller 处理。
5. 仍无 PDF 工具，交付为 Markdown。

### 当前状态

第 2 周 SR-4.2.1.2 编码**继续冻结**。
方案 05 第 6 节的 8 个问题预计次日全部得到答复，得到答复后再推进。

---

## 2026-09-17 · 建立项目安排与需规条目的对照关系

负责人：yoii1

### 起因

方案 05 写完后反映「做的事情有点杂，怕多做或少做」。核查确认这个感觉有依据：
《无线需规v3(3)》《项目安排》《技术参考》三份文件被平铺使用，
而它们实际回答的是三个不同问题——**做什么 / 什么时候做 / 怎么做**。
其中《技术参考》是合作方投标方案，合作方明确「不用完全按照里面来做」，仅标红必做；
《项目安排》是排期工具，本身不产生需求。只有需规是权威。

### 做了什么

**1. 新增 `项目安排_需规对照版.md`（517 行，仓库根目录）**

在《项目安排》原文基础上，为每周任务追加三个小节：

- ▎本周对应需规条目 —— 逐条列出功能要求 a)/b)/c)，标注归属（本线 / GIS 前端外包）
- ▎本周可参考《技术参考》 —— 标注标红（必做）与非标红（可选技术手段）
- ▎缺口提示 —— 该周涉及、但需规无对应条目的项

原文一字未改，新增部分独立成节，可与 PDF 逐段对照。

**2. 全文检索需规，建立缺口登记表（8 项 + 1 处编号缺陷）**

对 `无线需规v3(3).docx` 全部 1391 个段落提取文本后做关键词匹配：

| 关键词 | 命中 | 含义 |
|---|---|---|
| 编成 / 上级 / 逐级 | 0 / 0 / 0 | 通信层级约束在需规中无任何条目 |
| 层级 | 10 | 全部为 GIS 图层层级与资源树层级，与通信编成无关 |
| 分钟 | 0 | 需规无任何以分钟计的性能指标 |
| 失效 / 重规划 | 0 / 0 | 无节点失效后重规划的任务 |
| 预选 | 0 | 无「预选配置」概念 |
| 地形地貌场景 / SRTM | 0 / 0 | 无地形场景数量要求 |
| 衰落余量 | 0 | 无衰落余量规定 |

据此登记 8 项缺口，按优先级排序。**必须补的只有两项**：

- **通信层级约束**（技术参考 (9)，标红必做）→ 建议补入 SR-4.2.1.2
- **仿真规划时间 ≤5 分钟**（甲方验收指标）→ 建议补入 SR-4.2 h)，
  并修订 SR-4.2 g)。g) 原文「响应时间指标……**不含算法计算耗时**」，
  主动把算法耗时排除在性能指标之外，与甲方按 5 分钟验收直接冲突。

**3. 发现需规功能要求编号跳号**

SR-4.2.1.1 与 SR-4.2.1.2 的功能要求编号均为 a) b) c) e)，**d) 为空号**。
补充层级约束条款时正可占用 d)，无需重排现有编号。

**4. 方案 05 §4.8 标注为参考项**

§4.8 增量重规划对应技术参考 (8)，该条未标红且需规无对应条目。
已在节首加需规对照说明，标注为「参考项，非需规交付项，不计入关键路径工期」。
保留理由：层级树结构下实现成本极低（仅重算受影响子树）。

### 结果

- `项目安排_需规对照版.md` 517 行，覆盖六周全部需规条目
- 本线（算法与数据）六周需实现的需规条目：SR-4.2.1.1、SR-4.2.1.2、SR-4.2.2.1、
  SR-4.2.2.2、SR-4.2.3、SR-4.2.4、SR-4.2.5、SR-5.1～5.4、SR-6.1～6.3
- `python3 scripts/validate_data.py` 退出码 0，0 ERROR 0 WARNING

### 没做的

1. **需规本身未改。** 需规在文档线（contraller）名下，本线不跨线改动。
   上述两项必补条款需由 contraller 处理，已整理成可直接采用的条文。
2. **未生成 PDF。** 本机无 pandoc / wkhtmltopdf / weasyprint，
   当前交付为 Markdown。如需 PDF 可另出 HTML 由浏览器打印。
3. **原 `项目安排(1).pdf` 保留未动**，新文件为并列的对照版本。
4. SR-4.2.1.2 编码仍冻结，等评审。

### 待确认

- 通信层级条款补入需规的具体措辞与位置，需与 contraller 确认
- 甲方指标「态势刷新 ≤1 分钟」归 GIS 前端（外包），需向外包方明确

---

## 2026-09-17 · 据合作方澄清修订编成模型

负责人：yoii1

### 起因

就方案第 6 节问题 2（「不能跨层级」的边界）向合作方求证，得到四点澄清：

> 看那个 visio 图的线就行
> db 不能和 cdb 相连
> 是转了一层，通过背负式 db 电台或者那个车，可以连到 ii 或者 iii
> 那个 visio 图是另一条线，左边和右边有两个层级

此前只抽取了 Visio 图中的文字标签，**未解析连线**。据此补充解析
`visio/pages/page1.xml` 的 `Connect` 元素（35 个形状、36 条连接），还原实际拓扑。

### 还原出的拓扑

```
左支 · 编成层级（Ⅱ/Ⅲ 为双频节点，频段转换在节点内部完成）
  Ⅰdb固定站 ──同级── Ⅰdb机动站
      └── Ⅱdb/cdb固定站
             ├── Ⅱdb/cdb机动站 ─────┬── Ⅲdb/cdb机动站 ──┬── Ⅳcdb机动站
             │                     │                  └── Ⅳcdb机动站
             └── Ⅱdb/cdb机动节点站 ───┴── Ⅲdb/cdb机动站 ──┬── Ⅳcdb机动站
                                                       └── Ⅳcdb机动站
右支 · Ⅰ 直属短波机动力量（全 db，不接触 Ⅳ）
  Ⅰdb机动站 ├── a机动车×2  ──┬── 背负式db电台
            └── b机动车×12 ──┴── 背负式db电台
```

### 由此修正的三处（方案第 1.4、3.1、3.2、3.3、4.9 节）

1. **上一版的跃迁表是错的。** 曾把「Ⅳ → Ⅱ」列为允许跃迁，
   系对标红段「确保可以通过超短波连接到Ⅱ或Ⅲ」的误读——
   该句主语是右支的短波中继资产，不是 Ⅳ。图中 4 条 Ⅲ──Ⅳ 连线，**无一条 Ⅱ──Ⅳ**。
   已更正为：正常工况下 Ⅳ 的上级固定为 Ⅲ。
2. **频段约束的地位提升。** 「db 不能和 cdb 相连」是装备约束，
   任何工况下都不放宽；上一版把作战损伤工况写成「仅物理可行」不准确，
   放宽的只有组织意义上的编成约束。由此直接推出：
   **Ⅳ（仅 cdb）与 Ⅰ（仅 db）无论距离多近都不可能直连。**
3. **可部署装备中只有 Ⅲ 机动站带超短波。** a 车、b 车、背负式电台均为短波，
   **不能接 Ⅳ**。因此 Ⅳ 接入不上时唯一补救手段是增设 Ⅲ 机动站，
   这使 P1 第 1 层成为单一类型的集合覆盖，第 2、3 层才是多类型加权选择。

### 一个此前提出的冲突得到缓解

上一版指出「严格层级即树形，与 SR-4.2.4 抗毁性指标冲突」。
解析连线后发现**参考编成本身就不是纯树**：两个 Ⅲ 各自双上级（挂在 Ⅱ机动站与Ⅱ机动节点站下）、
两部背负式各自双上级（挂在 a 车与 b 车下）、Ⅰ 固定站与机动站同级互备。
**多归属是参考设计里已有的做法**，不需另行发明。
仍未解决的是 Ⅳ 层——图中每个 Ⅳ 只挂一个 Ⅲ，要 Ⅳ 层也多归属须增设 Ⅲ 机动站，
成本直接上升，已作为方案 1 / 方案 3 的对比项呈现。

### 新增待确认的三点（方案第 6 节问题 2）

- Ⅱ 带 cdb 设备 ×2，物理上可与 Ⅳ 建超短波链路，Ⅲ 不可达时**是否允许** Ⅳ 直挂 Ⅱ？
- 同级链路（Ⅱ──Ⅱ、Ⅲ──Ⅲ）能否用于**中继转发**，还是仅用于协同？
- 图中「×6」「Ⅰ型×2、Ⅱ型×6」是**编成定额还是容量上限**？
  若为容量上限，问题变为**有容量的**多级设施选址，算法需相应调整。

### 结果

方案文档 765 → 842 行。**仍未编码。**

---

## 2026-09-17 · 按通信层级要求重写部署规划方案（第二版）

负责人：yoii1

### 做了什么

依据合作方《技术参考》标红的通信层级要求，重写
`docs/design/05_移动电台部署规划算法方案.md`。文档从 612 行增至 765 行。
**仍未编码，等审查。**

### 核心变化：建模从 Steiner 森林换成多级设施选址

第一版把 P1 建模为**无向传播可达图上的节点权重 Steiner 森林**，
理由是集合覆盖无法表达「两个中继必须串联才生效」、此时贪心会停滞。

层级约束成立后这个理由不再适用，且问题**变得更好解**：

1. 需求结构从「任意点对连通」收敛为「所有 Ⅳ 汇向同一个根 Ⅰ」，是汇聚树而非一般森林；
2. 路径长度被层级数限定（Ⅳ→Ⅲ→Ⅱ→Ⅰ 最多两跳中转），
   导致贪心停滞的「长中继链」结构被层级规则排除；
3. 问题按层解耦，每层是「让本层节点接入上一层」的**集合覆盖**，
   目标函数单调子模，贪心有 1−1/e 近似保证；一般 Steiner 森林没有这个性质。

故本版建模为**多级无容量设施选址（MLUFLP）**，求解改为**自底向上分层贪心 + 层间迭代修正**。

### 其他实质改动

| 项 | 内容 |
|---|---|
| 链路可行性 | 由单一物理判据改为**两重判据**：物理可行 + 编成可行。系统维护两张矩阵，正常矩阵是损伤矩阵的子集 |
| 频段绑定 | Ⅳ 接入必为超短波、Ⅱ/Ⅲ 上联 Ⅰ 必为短波，由编成图的装备配置推出 |
| 决策变量 | 由「选哪个位置」改为「（位置，类型）对」——可部署装备有 Ⅲ 机动站 / a 车 / b 车 / 背负式四类，功率从 400 W 到 20 W 相差 26 dB，非同质决策 |
| 任务满足判据 | 由「点对连通」改为「每个 Ⅳ 存在逐级上行链到 Ⅰ」；Ⅳ 间横向通联由上行再下行实现 |
| 新增章节 4.8 | 增量重规划，对应《技术参考》(8) 的 20% 节点失效下 ≤3 分钟。层级结构在此是优势：失效只影响子树，无需全网重算 |
| 新增章节 4.9 | 跨层级降级。标红段「必要时可以跨层级通信（存在作战损伤时）」的落点：不是常态能力，而是损伤工况下切换到损伤矩阵，且降级链路须显式标注 |
| 工期 | 4 天 → 5.5 天 |

### 指出的一处需求冲突

**严格层级与 SR-4.2.4 抗毁性指标相互冲突。** 严格层级即树形结构，
树的每个内部节点都是其子树的单点故障源；而 SR-4.2.4 要求
「单链路断开时关键通联关系连通率 ≥90%」——断开任一 Ⅱ→Ⅰ 链路会使整棵子树失联，
若该子树承载的关键通联超过 10%，该指标在正常工况下不可能达成。

方案中给出两条出路（多归属 / 在损伤工况下评估抗毁性），并列为待请教问题第 4 条。

### 待请教问题由 8 条调整为 8 条（新增 3 条、合并 3 条）

新增：第 2 条「不能跨层级」的确切边界（标红段一面说逐级上联，
一面又允许 Ⅳ 直连 Ⅱ 而越过 Ⅲ，存在歧义）；第 3 条现有 411 条任意点对通联需求
与层级模型的关系（废弃改由编成导出，还是保留作为额外约束）；
第 4 条上述抗毁性冲突。
第 8 条补充 P.1546 与 P.1812 的取舍（《技术参考》与 `CLAUDE.md` 不一致）。

### 结果

- 全文 12 处引文经脚本逐字比对，与三份源文档完全一致（甲方要求、软需、技术参考）
- 保留第一版的 2.4 节（候选点筛选）不变，该部分已实现且不受层级影响

### 没有做的

- **未编码**，等审查结论。
- **未改动任何数据或代码**。层级落地的第一个前置是给 `node.csv` 补
  `echelon` 与 `parent_node_id` 两个字段并同步三处定义，已写入方案第 7 节工期第 0 步。
- 现有生成数据中短波/超短波按比例随机分配，与编成图（Ⅳ 仅超短波、Ⅰ 仅短波）不符，待一并调整。

---

## 2026-09-17 · 合作方《技术参考》入库

负责人：yoii1

### 做了什么

合作方 2026-09-17 提供《技术参考》，已存入 `docs/input/技术参考.docx`。
合作方说明：文件内技术路线**可参考，不必完全照做；但标红处必须实现，
其中「通信层级」在做网络规划时必须考虑进去**。

### 标红（必须实现）的五处

1. **（9）网络规划：正常情况下不能跨层级直接通信，只能一级一级往上通信**
2. 四级编成的配置流程与连接规则（全段标红，原文见文件）
3. a）短波天波传播模型
4. b）超短波视距传播模型
5. a）干扰源标绘与参数设置

### 通信层级结构（标红段落 + 文档内嵌 Visio 图）

层级自上而下为 **Ⅰ → Ⅱ → Ⅲ → Ⅳ**，Ⅳ 为任务站点、位于通信链路最下层。
连接规则：

- 正常情况**逐级向上**，不得跨层级直连；
- Ⅳ 任务站点用**超短波**接入 Ⅱ 或 Ⅲ，再经其**短波**设备上联至 Ⅰ；
- 任务站点若无到基地的链路，则规划 Ⅲ 机动站，或 Ⅰ 直属的 a / b 机动车、
  背负式短波电台作为中继；
- **仅在存在作战损伤时允许跨层级通信**。

文档内嵌的 Visio 图（`word/embeddings/Microsoft_Visio_Drawing.vsdx`，
在 Word 里双击才能看到）给出了编成与装备参数。图中 `db` 指短波、`cdb` 指超短波：

| 编成 | 装备与参数 |
|---|---|
| Ⅰ db 固定站 / 机动站 | 短波 400 W；天线：鞭、三环、NVIS、双极 |
| Ⅱ db/cdb 固定站 / 机动站 | 短波 ×1（参数同 Ⅰ）+ 超短波 ×2，超短波均 1.5 m 鞭天线、50 W |
| Ⅱ db/cdb 机动节点站 | Ⅰ 型 ×2、Ⅱ 型 ×6；短波 ×1 + 超短波 ×5（1.5 m 鞭，50 W） |
| Ⅲ db/cdb 机动站 | 短波 ×1 + 超短波 ×4（1.5 m 鞭，50 W） |
| Ⅳ cdb 机动站 | ×6，仅超短波 |
| a 机动车 ×2（Ⅰ 直属） | 短波 400 W；鞭、三环、NVIS、双极 |
| b 机动车 ×12（Ⅰ 直属） | 短波 125 W；NVIS 天线、宽带天线 |
| 背负式 db 电台 ×12 | 短波 20 W；鞭天线（15–20 km）、倒 V 天线（100 km） |

### 对现有工作的影响（**需要决策**）

**这条要求改变了网络规划的模型本身**，不是新增一个约束项那么简单：

1. **数据模型缺字段**。`node.csv` 现有 `node_role`、`mobility`，但**没有层级与上级归属**。
   要支持逐级上联，节点至少需要 `echelon`（Ⅰ/Ⅱ/Ⅲ/Ⅳ）与 `parent_node_id`。
   字段一改需同步三处（生成脚本、schema 模板、数据字典）。
2. **链路可行性判据要加一层**。当前判据只看传播是否可达（链路余量 ≥ 门限）。
   加入层级后，**传播可达但跨层级的链路属于不允许建立**，除非处于作战损伤场景。
   这会显著改变拓扑结构与连通性计算。
3. **直接影响审查中的 SR-4.2.1.2 方案**。`docs/design/05_...md` 第 3 节把问题建模为
   「在无向图上满足通联需求的最少中继放置」，其中图是传播可达图。
   层级约束使该图变为**分层有向结构**，P1 的 Steiner 森林建模需要重新审视。
   **建议把这份《技术参考》连同层级要求一并提交给大师姐，与那 8 个问题一起判断。**
4. **频段与层级绑定**。Ⅳ 仅超短波、Ⅱ/Ⅲ 兼有短波与超短波、Ⅰ 为短波。
   当前生成数据中短波/超短波节点是随机分配的，与该结构不符。
5. **规模口径可能需要复核**。图中给出的是实际编成（Ⅳ×6、b 机动车 ×12、背负式 ×12 等），
   与我们现用的「短波 ≥50、超短波 ≥100」验收口径量级不同，两者关系待确认。

### 文档中其他值得注意但未标红的内容

- **新增性能指标**：（8）节点失效情况下（20% 节点失效）仿真规划时间 **≤3 分钟**，
  采用增量重规划、仅对受影响子网局部重算。此前我方文档未记录此指标。
- 传播模型建议为 ITU-R **P.533**（短波天波）、ITU-R **P.1546** + **Longley-Rice ITM**（超短波）。
  注意 `CLAUDE.md` 现写的是 P.526/P.1812/P.533/P.368，与此处的 P.1546 不一致，待统一。
- 覆盖计算：快速模式 500 m 栅格单站 <5 s，精确模式 100 m 栅格单站 <30 s。
- 衰落余量 6–12 dB，边缘覆盖率 ≥90%；功率优化在 SNR ≥10 dB 约束下最小化总辐射功率。
- 频率分配建议 Dsatur 贪心 + 遗传算法全局优化。
- 技术选型建议（Neo4j、Drools、XGBoost、BiLSTM-CRF、D3.js、OpenLayers）均未标红，属「可参考」。

### 没有做的

- **未按层级要求改动任何代码或数据**，等决策。
- 未将该文件内容纳入 SRS —— `docs/input/` 属文档线目录，
  已入库但**需告知 contraller**，其中（8）的 ≤3 分钟指标与层级要求可能影响需规章节。

---

## 2026-09-16 · 两份师弟任务单 + 故障案例列错位修正

负责人：yoii1

### 做了什么

在等待 SR-4.2.1.2 方案审查结论期间，整理两件**不依赖该审查**的工作，分派给两位同学。

#### 任务单一：`docs/design/06_设备参数补全任务单.md`

起因是检查采集到的型号表时发现关键参数大面积缺失：

| 表 | 条数 | 缺失情况 |
|---|---|---|
| 天线型号 | 78 | **增益缺 76 条（97%）**；水平/垂直波束宽度、可用挂高全缺；极化缺 76 条 |
| 设备型号 | 16 | 整机功耗全缺；接收灵敏度缺 4；信道带宽缺 6；其中 `VUHF005`、`VUHF003` 近乎全缺 |

天线增益与接收灵敏度是链路预算的必需项，缺失导致全系统仍在用构造型号
（生成脚本输出 `CONSTRUCTED`）。任务单给出逐条缺失清单、单位换算表
（W→dBm、μV→dBm、MHz→kHz）、按天线类型的典型增益兜底表、格式统一要求与验收标准。
补齐后放回 `data/raw/equipment_docs/` 重跑生成脚本即自动切换，无需改代码。

#### 任务单二：`docs/design/07_故障知识图谱构建任务单.md`

`data/fault/knowledge_graph/` 为空，口径要求 500–2000 条三元组，是第 5–6 周故障诊断的前置。
任务单含：本体的实体类型与关系类型、**关系搭配表**（每种关系允许的 head_type→tail_type，
防止产出无法串联的三元组）、从一条真实案例展开出 9 条三元组的完整示例、
工作量分配（故障域 1200–1500 + 设备域 200–300）、四条质量要求与验收标准。

### 字段口径决定

三元组此前有两套互相冲突的定义，现已统一为**带实体类型的那套**：

```
triple_id, head, head_type, relation, tail, tail_type, weight, source_doc
```

`data/schemas/fault_triples_template.csv` 已改写，与 `docs/design/01_数据字典.md` §3.5 一致
（已用脚本逐字段比对）。理由：不带类型时「功放模块」无法区分是部件（Component）
还是原因（Cause），SR-6.3 要求的图谱推理无法进行。

### 顺带修正：导入的故障案例列错位

检查 KG 原料时发现 `fault_case_imported.csv` 240 行**全部列错位**：
行尾多余逗号使表头多出一个空列，内容整体右移——
现象落在 `fault_component`、原因落在 `diagnosis_steps`、诊断步骤落在 `solution`、
处置方案落在 `repair_time_min`、来源落在末尾空列。

（导入批次 README 称「已修正字段错位问题」，但实际未修正或修正方式不同。）

错位在 240 行上完全一致，已按位置重映射修正，并把修正逻辑写进
`scripts/prepare_geodata.py` 的 `fix_fault_cases()`，重跑预处理即得到正确文件。
同时把 `object_type` 的 7 种原始取值（`RADIO_SET` / `RADIO_LINK` / `RADIO_SYSTEM` 等）
归并到数据字典约定的三类：DEVICE 167 / NODE 37 / LINK 36。

### 结果

- `gen_test_data` / `gen_algorithm_design_data` / `validate_data` 全部通过，0 ERROR / 0 WARNING
- `tests/` 三个用例全部 PASS

### 没有做的

- **SR-4.2.1.2 仍未编码**，等审查结论。
- 故障案例原文为英文（FM24-19 等美军野战手册），实体名称的中译作为 KG 任务的一部分交由同学完成。
- `repair_time_min` 原列被内容占用，真实案例实际没有处置耗时数据，已置空。

---

## 2026-09-16 · 方案文档补充候选点筛选章节

负责人：yoii1

在 `docs/design/05_移动电台部署规划算法方案.md` 新增 **2.4 候选部署位置是怎么选出来的**，
原 2.4 顺延为 2.5。内容为候选点筛选的完整说明，不作改动前后对比，只讲当前做法与结果：

- 需规依据（SR-4.2.1.1 主事件流与功能要求的逐字引文，已比对一致）
- 四步流程：遍历规划区域 → 漏斗式施加约束 → 点位优化处理 → 打分与分级
- 六项约束的判据、单点代价与需规依据对照表
- 距路距离场的实现（道路栅格化 + 精确欧氏距离变换）
- 评分四项的权重、形式与理由（高程取 sqrt 是因为视距随高程平方根增长）
- 筛选结果：筛除统计、分级、用途、各项分位数、高程分层、地物构成、6×6 空间分布、分数前 6 名
- 两点结果解读：距路中位数为 0 的原因；候选点高程中位数只有 43 m 的原因
- 五个可调参数及其影响

文档现 612 行。

---

## 2026-09-16 · 重做候选点筛选（SR-4.2.1.1）

负责人：yoii1

### 起因

在为 SR-4.2.1.2 方案梳理输入时发现，候选点其实是**占位实现**：

```python
# 原 scripts/gen_algorithm_design_data.py
lon = RNG.uniform(lon0 + 0.02, lon1 - 0.02)   # 在规划区内随机撒 120 个点
road_distance_m = RNG.uniform(80, 4800)       # 距路距离是随机数，不是算出来的
```

地形（高程/坡度/地物）读的是真实 DEM，但**撒点方式与交通可达性都是假的**。
需规 SR-4.2.1.1 主事件流写的是「系统调用地形分析能力，**遍历规划区域**筛选符合条件的点位」，
而当时是随机抽样；「交通可达性」在仓库已有 76 239 条真实道路的情况下仍用随机数。

这会直接影响 SR-4.2.1.2：部署算法从候选点里选位置，候选点若是车开不到的地方，
算得再准也没有意义。

### 做了什么

新增 `scripts/planning/`：

| 文件 | 内容 |
|---|---|
| `roads.py` | 距最近道路的距离场 |
| `candidate_sites.py` | 遍历扫描、漏斗筛选、空间稀释、打分分级、放宽建议 |

**距路距离场**：把 76 239 段道路按 250 m 栅格化（折线顶点间距大于栅格时插值补点，
避免道路出现断点），再做**精确欧氏距离变换**（Felzenszwalb & Huttenlocher
的 O(N) 一维抛物线下包络算法，逐列再逐行两遍）。之后任意点查距路距离为 O(1)。
构建 1.1 s，521 × 521 格。抽查：西部山区 1250 m、山前 500 m、平原 0–250 m。

**候选点扫描**：500 m 网格遍历 58 081 个格点，约束按**从廉价到昂贵**的漏斗顺序施加，
尽早短路——高程(1 次采样) → 地物(1 次) → 交通可达性(O(1) 查表) → 禁部署区 →
坡度(4 次) → 通视(每目标 48 次)。耗时 3.7 s。

**点位优化处理**（需规主事件流第 4 步）：按最小站间距 3 km 做贪心空间稀释，
用网格桶做邻域查询避免两两比距。

**评分**：高程按 sqrt 给分（视距随高程平方根增长）、坡度、距路、与已有节点距离，
四项权重集中在 `score_site()` 一处定义，便于解释与调整。

### 结果

候选点由 120 个（随机）变为 **1137 个**（真实扫描），全部通过约束。

```
扫描格点 58081，筛除 spacing=43802  landcover=8745  slope=3236  road=1159  elevation=2
```

- 高程 23–1566 m，其中 ≥300 m 的 142 个；坡度中位 0.6°；地物：耕地 879 / 草地 131 / 林地 107 / 裸地 20
- `candidate_role` 改为按点位特征判定（高地远离节点 → RELAY；紧邻道路且平坦 → MOBILE_STATION），
  不再按序号轮流分配
- `gen_test_data` / `gen_algorithm_design_data` / `validate_data` 全部通过，0 ERROR / 0 WARNING
- `tests/` 三个用例全部 PASS

### 一个容易踩的坑，已避开

**候选点密度只能用最小站间距控制，不能按分数截断取前 N 个。**
评分里高程权重较高，按分数排序后前列全是西部山区的点；若截断取前 N，
东部平原会一个候选点都不剩，区域覆盖无从谈起。
实测按站间距稀释后，六个经度带的候选点数为 178 / 180 / 201 / 185 / 194 / 199，分布均匀。

### 顺带修的

- `tests/test_candidate_sites.py` 原先硬编码 `candidate_count == 120`，
  改为对齐数据集实际条数，不再写魔法数字。
- 原判定里的禁用地物 `山地岩石` 在真实地物映射后已不存在，属死代码，已随重写移除。
- `candidate_sites_template.csv` 与样例响应已同步。

### 对 SR-4.2.1.2 方案的影响（已更新 `docs/design/05_...md`）

候选点从 88 个可部署变为 1137 个，链路可行性矩阵的规模随之变化：

| | 原方案（按 88 候选点估） | 现在（实测） |
|---|---|---|
| 链路对数量 | 1.76 万 | **82.3 万** |
| 单条链路耗时 | 估 | **实测 37 μs** |
| 矩阵构建耗时 | 估 2–4 s | **约 30 s** |
| 全流程合计 | 估 12–25 s | **约 80–110 s** |

对甲方「仿真规划时间 ≤5 分钟」仍有约 3 倍余量。
若需压缩，可把生成阶段的最小站间距由 3 km 放宽到 5 km，
候选点降至约 379 个、耗时降至约 4 s——这是可调的精度/耗时折中，已写入方案文档。

### 没有做的

- **SR-4.2.1.2 仍未编码**，等大师姐审查结论。
- 服务层仍是「读取预生成候选池 + 请求级过滤」，而非请求时实时扫描。
  按需规主事件流应为实时扫描；当前扫描仅 4.8 s，改为实时可行，但会改动服务结构，暂未动。
- 通视约束（`los_required_node_ids`）已实现但生成阶段默认未启用（目标节点清单为空）。

---

## 2026-09-16 · 提交部署规划算法方案，待审查

负责人：yoii1

### 做了什么

编写 `docs/design/05_移动电台部署规划算法方案.md`，供领域专家评审。
**评审通过前不动手编码。**

文档结构：

1. **原始任务目标**——甲方《【非密】短波规划要求》与《无线需规v3(3)》的**逐字引文**
   （已用脚本逐条比对，11 处引文与源文档完全一致，无改写）；
2. **现有条件**——真实 DEM/地物/道路规格、已有模块、已生成数据，
   以及**明确没有的东西**（无 ILP 求解器、传播模型仍为占位实现、真实设备型号未并入）；
3. **问题的数学表述**——符号、链路可行性判据、需求满足判据、覆盖率判据，
   以及 P1（最少电台数）与 P2（给定数量求最优位置）两个子问题的形式化；
4. **代码方案**——模块划分、四步算法、时间预算；
5. **验证方案**；
6. **8 个待请教的问题**。

### 从甲方原文里挖出的两条硬指标

此前未在本线文档中记录，直接约束算法设计：

- **仿真规划时间 ≤ 5 分钟** —— 算法的计算预算
- **规划预选配置 ≥ 3 种** —— 必须输出至少 3 套可对比的方案

方案中已按此设计：预计算链路可行性矩阵与覆盖位图后，
搜索过程全部退化为位运算，预估总耗时 12–25 秒，对 5 分钟有 10 倍以上余量。

### 算法要点（供交接）

- P1 建模为**节点权重 Steiner 森林**而非集合覆盖。理由：集合覆盖建模无法处理
  「两个中继必须串联才能连通一对端点」的情形，此时所有候选点边际增益为 0，贪心停滞。
- 覆盖计算采用**径向视域（R3 变体）**而非逐格点传播计算：
  88 候选 × 180 射线 × 160 步进 ≈ 253 万次采样，比逐格点少一个数量级。
- 位图用 Python 大整数（`|` / `&` / `bin(x).count('1')` 由 C 实现），避免第三方依赖。
- 因无 ILP 求解器，启发式必须自证质量：方案给出**两个下界**（分量合并下界、覆盖下界）
  并报告 gap，另在小规模（|S|≤18、p≤3）用穷举求精确最优做回归对照。

### 没有做的

- **未编码**，等评审结论。
- 第 6 节 8 个问题中，以下三个可能改变方案结构，需优先答复：
  第 1 条（是否为精确求解破例引入 ILP 求解器）、
  第 2 条（P1 的 Steiner 森林建模是否恰当）、
  第 7 条（与 SR-4.2.4 多目标优化的职责边界，是否存在重复优化）。
- 「满足任务区域覆盖要求」在需规中无量化定义，方案中拟定为
  1 km 栅格、链路余量 ≥6 dB、覆盖率阈值默认 0.9，**属自拟，需确认**。

---

## 2026-09-16 · 移除汇报出图代码

负责人：yoii1

### 做了什么

汇报出图属于汇报线的工作，不属于代码与数据线，按约定从本仓库移除：

| 删除 | 体积 |
|---|---|
| `scripts/gen_visualizations.py` | 797 行 |
| `docs/visualization/`（19 张 PNG + `map_overview.html` + `summary.json` + `README.md`） | 4.2 MB |

**保留 `progress_report/week1.pptx`。** 已确认该 pptx 内嵌了 13 张图片（1.9 MB），
图片打包在文件内部，删除 `docs/visualization/` 不影响汇报稿的正常打开与展示。

删除原因不止是分工：这份出图代码依赖 `matplotlib` / `numpy` / `pandas` / `rasterio`
四个第三方包，与 CLAUDE.md「只用标准库」冲突；且它读取的
`data/imported/.../raw/*.tif` 已随原始解压件删除，输入缺失时会**静默跳过**地形类图表
（`terrain_available: false`）而不报错，留在仓库里反而容易误导。

### 结果

- 删除后 `gen_test_data.py`、`gen_algorithm_design_data.py`、`validate_data.py`
  全部通过，0 ERROR / 0 WARNING，退出码 0；`tests/` 三个用例全部 PASS。
- 已确认除 `工作交接.md` 外无其他文件引用被删内容，不存在断链。
- 仓库中的算法与数据代码恢复为**纯标准库**：`scripts/` 下仅剩
  `terrain.py`、`propagation.py`、`gen_test_data.py`、`gen_algorithm_design_data.py`、
  `prepare_geodata.py`、`validate_data.py`、`datapaths.py` 与 `geo/` 三个读取模块。
  唯一的第三方依赖只剩 `algorithm-service/requirements.txt` 的 HTTP 层（待决定）。

### 说明

历史版本仍保留在 git 中，如需找回：`git show 857c602:scripts/gen_visualizations.py`
（或从 `f9392c7` 及更早的提交检出 `docs/visualization/`）。

---

## 2026-09-16 · 规划区西移并切换到真实地形

负责人：yoii1

### 做了什么

上一节提出的 bbox 问题经确认走**方案 B**：在已下载的数据范围内把规划区西移 20 km，
不要求重新采集。同时把地形源从合成地形切换到真实 DEM —— 两件事都会触发全量数据重生成，
合并做只折腾一次。

| 项 | 改前 | 改后 |
|---|---|---|
| 规划区 bbox | `114.2724, 36.7610, 115.6276, 37.8390` | `114.0465, 36.9110, 115.4045, 37.9890` |
| 地形源 | `SyntheticTerrain`（合成） | `DemTerrain`（真实 DEM + 地物栅格） |
| 规划区高程 | 17–526 m，中位 35 m | **16–1616 m** |
| 坡度 >15° 占比 | 0.7% | **8.7%** |

具体改动：

1. **`scripts/terrain.py` 重写**。新增 `DemTerrain`（基于 `GridRaster` 读真实网格）与
   `default_terrain()` 工厂：真实数据存在则用，缺失自动回退合成地形并给出提示。
   `SyntheticTerrain` 保留，作为无数据环境的回退与冒烟测试用。
   **对外接口未变**（`elevation` / `slope_deg` / `landcover` / `profile` /
   `haversine_m` / `los_clearance`），`propagation.py` 一行未改。
2. **地物编码归并**。真实地物是 11 类数值编码（10 树木 / 40 农田 / 80 永久水体 …），
   而 `propagation.py` 的损耗表以「林地/草地/耕地/建成区/裸地/水域」为键。
   在 `DemTerrain.landcover()` 内做一次映射，使真实数据接入后传播模型无需改动。
3. **任务区重新布置**。原四个任务区按旧框设定，现改为覆盖山地、山前过渡与平原三类地形
   （太行山区 / 山前过渡区 / 中部平原区 / 东部平原区），使部署与传播计算落在有区分度的地形上。
4. **消除 bbox 硬编码**。`validate_data.py` 与 `prepare_geodata.py` 改为从
   `terrain.BBOX` / `terrain.BBOX_DATA` 取值，不再各自硬编码，避免再次漂移。
5. **同步下游**。`algorithm-service/app/candidate_sites.py` 的 `DEFAULT_AREA_BBOX`、
   `docs/api/sample_requests/candidate_sites_request.json` 的 `area_bbox` 与
   `elevation_range_m`、`docs/api/sample_responses/candidate_sites_response.json` 均已更新。
6. **15 个 schema 模板全部与实际输出对齐**（CLAUDE.md 要求的三处同步）。

### 结果

- `gen_test_data.py`、`gen_algorithm_design_data.py`、`validate_data.py` 全部通过，
  **0 ERROR / 0 WARNING**，退出码 0。
- `tests/test_candidate_sites.py` 三个用例全部 PASS。
- 真实 DEM 载入 0.2 s；沿 37.45°N 实测：114.08° 高程 1201 m 坡度 24.6° 林地，
  115.38° 高程 23 m 耕地 —— 地形与地物均来自真实数据。
- 候选点可部署数 79/120，筛除原因分布合理（距路 29、间距 9、坡度 4、地物 2）。
- 数据规模仍全部达标：节点 156（短波 52 / 超短波 104）、链路 500、通联需求 411、
  链路状态记录 2500、故障案例 240；必要通联关系 69 条两端全部连通。

### 过程中发现并修掉的两个问题

1. **样例请求的 `elevation_range_m` 下限 50 m 会误杀候选点**。合成地形整体偏高，
   这个下限一直没暴露；真实平原只有 23–35 m，直接滤掉 62 个候选点。已改为 20 m。
2. **`fault_rules_template.csv` 表头与实际输出不符**（模板来自 `d1f5aee`，
   数据来自 `563dc68`，非本次引入）。已按实际输出同步。

### 没有做的

- ~~`scripts/gen_visualizations.py` 已失效~~ **已于 2026-09-16 连同 `docs/visualization/` 一并删除**
  （汇报出图属于汇报线的工作，不在代码与数据线范围内），见本文件最上一节。
- **西侧无缓冲**。规划区西边界与 DEM 西边界重合，贴西边界链路的遮挡计算偏乐观。
  如需严格处理，需补采西延约 20 km 的 DEM（约 3 MB）。
- 真实设备型号仍未并入，`gen_test_data.py` 仍输出 `CONSTRUCTED`（表头与约定不一致，需先对齐字段）。
- 故障知识图谱三元组（500–2000 条）仍为空。
- `algorithm-service/requirements.txt` 引入 fastapi / uvicorn / pydantic，
  与 CLAUDE.md「只用标准库」冲突；核心算法本身是纯标准库，依赖只在 HTTP 层。待决定是否换成 `http.server`。

---

## 2026-09-16 · 导入数据预处理并入库

负责人：yoii1

### 做了什么

数据采集同学 9/11 交付的 `data.zip`（643 MB，解压 2.06 GB）此前只存在于做导入的那台机器上，
`.gitignore` 排除了 `raw/` 与 `source/*.zip`，未同步到其他机器。本次把它处理成可入库的形式。

1. **删除解压出的 41 份 PDF**（211 MB）。均为厂商版权资料（Motorola、Barrett、Harris、Yaesu 等），
   不适合公开分发；原件完整保留在 `data.zip` 内，仓库中只留 `docs_index.csv` 索引。
2. **写了三个纯标准库的地理数据读取模块**（`scripts/geo/`）：
   - `tiff.py` —— 最小 GeoTIFF 读取器，含手写的 TIFF 变体 LZW 解码（early change、9–12 位变长码），
     支持条带与分块两种布局
   - `shapefile.py` —— 最小 Shapefile / DBF 读取器，PolyLine 几何 + 属性表
   - `grid.py` —— 网格读取与经纬度 O(1) 采样
3. **`scripts/prepare_geodata.py`** 一次性转换，产出进仓库的 28 MB：

   | 数据 | 原始 | 入库 | 处理 |
   |---|---|---|---|
   | DEM | 17.2 MB LZW 分块 GeoTIFF | 20 MB | 解码平铺 + gzip |
   | 地物 | 356.7 MB **未压缩** GeoTIFF 10 m | 4.1 MB | 3 倍降采样至约 28 m + gzip（压缩 87 倍） |
   | 道路 | 49.3 MB Shapefile（GBK） | 4.9 MB | 裁框、仅留车行等级、属性精简、转 UTF-8 + gzip |
   | 故障案例 / 资料索引 | — | 64 KB | 去 BOM |

4. **`data/raw/README.md`** 记录来源、处理方式、读取方法与待确认事项。
5. 原始 `data.zip` 移回本机桌面；解压出的 `raw/` 已删除（可从 zip 重新解出）。

### 结果

- 2.06 GB → **28 MB**，最大单文件 20 MB，远低于 GitHub 100 MB 限制，无需 Git LFS。
- 读取零第三方依赖，符合 CLAUDE.md「只用标准库」。已验证：
  `dem.sample(114.30, 37.30) -> 263 m`、`lc.sample(...) -> 30 草地`、越界返回 `None`。
- DEM 全幅高程 10–1676 m，NoData 0 个，地理配准与边界校验通过。
- 平铺 gzip 加载 0.1 s，原 LZW GeoTIFF 需 5.5 s（多 1.4 MB 换 55 倍速度）。
- 本次未改动数据生成逻辑，`gen_test_data.py` + `validate_data.py` 复跑仍为 0 ERROR / 0 WARNING。

### 发现的问题：规划区 bbox 选错了（**需要决策**）

我在 9/8 定规划区时判断「一个框内同时有太行山 1000–1800 m 与华北平原」，
真实 DEM 到位后实测，**这个判断是错的**：

| 经度带 | 高程中位 | 最大 | >500 m 占比 |
|---|---|---|---|
| **114.047–114.272（外扩缓冲带）** | **396 m** | **1526 m** | **33%** |
| 114.272–114.498 | 132 m | 527 m | 0% |
| 114.498 以东 | 30–53 m | ≤152 m | 0% |

太行山主脊全部落在 `114.047–114.272` 这条**外扩缓冲带**内，而规划区西边界正是 `114.2724`，
恰好卡在山脚下，往东全是华北平原。当前规划区实测：高程 18–524 m、中位 35 m、
坡度中位 2.2°、坡度 >15° 仅 0.7%。

影响：候选点筛选的坡度约束几乎筛不掉任何区域；SR-4.2.3 要求的地形遮蔽不规则覆盖
在平原上体现不出来，超短波覆盖会接近规则圆形 —— 而软需明确要求不得用圆形近似。

三个选项（已用真实 DEM 实测）：

| 方案 | 高程 | >500 m | 坡度 >15° | 代价 |
|---|---|---|---|---|
| A 维持现状 | 18–524 m | 0.0% | 0.7% | 无，但地形基本不起作用 |
| **B 规划区西移至现有 DEM 最西端**<br>`114.047–115.402, 36.940–38.018` | **16–1616 m** | **7.2%** | **8.7%** | 不用重下数据；西侧无 20 km 缓冲 |
| C 以主脊为中心重选（中心约 114.45, 37.30） | — | — | — | 现有 DEM 只覆盖 80%，需重下 DEM/地物/道路 |

我倾向 **B**：不用让同学重新采集，地形起伏立刻可用。代价是西边界外没有缓冲，
贴着西边界的链路拿不到更西侧的遮挡数据，覆盖会偏乐观 —— 但西边界外就是山西山区，
真实任务里本来也不会把节点部署到那边。若要彻底干净则选 C，需重下约 20% 的数据。

**此项需你确认后我再改。** 改 bbox 会触发节点重新布设与全量数据重生成，
并需同步 `docs/design/01_数据字典.md`、`docs/design/02_数据采集任务单.md` 与 `data/raw/README.md`。

### 没有做的

- **规划区 bbox 未改**，等上述决策。
- **真实设备型号未并入**。`data/raw/equipment_docs/` 仍是示例行，
  `gen_test_data.py` 仍输出 `CONSTRUCTED`。采集到的 16 个设备型号与 77 个天线型号
  表头与现有约定不一致（ID 形如 `HF001` 而非约定的 `DM-0001`，且存在空字段），
  并入需先对齐字段并同步三处定义，会触发全量数据重生成，改动较大，单独处理。
- **`terrain.py` 仍使用 `SyntheticTerrain`**。真实 DEM 的读取能力已具备（`GridRaster`），
  但未切换默认地形源 —— 切换会改变全部生成数据，应与 bbox 决策一并进行。
- 故障知识图谱三元组（500–2000 条）仍为空，是本线的欠账。

---

## 2026-09-08 · 统一枚举口径

负责人：yoii1

### 做了什么

上一节第五部分提出的跨线枚举冲突已确认结论：**沿用当前代码口径**。
据此改写 `data/dictionary/data_dictionary.md` 的「通用枚举」一节：

| 概念 | 改前 | 改后 |
|---|---|---|
| 网络类型 | `network_type`：`HF` / `VHF` | `device_class`：`HF` / `VUHF` |
| 节点类型 | `node_type`：`fixed` / `mobile` / `relay` | `node_role`：`TASK` / `FIXED_STATION` / `RELAY` / `CANDIDATE`，另有 `mobility`：`FIXED` / `VEHICLE` / `MANPACK` |
| 状态 | `status`：`normal` / `degraded` / `fault` / `offline` | `status`：`NORMAL` / `FAULT` / `OFFLINE`；`degraded` 归入 `affect_state` |

同时补齐了该文件原本缺失的枚举（`link_state`、`affect_state`、`impact_level`、
`fault_type`、`interference_type`、`task_priority`）、布尔与缺失值写法、单位约定，
并注明字段语义的权威说明在 `docs/design/01_数据字典.md`。

原有「待确认事项」保留未动，另把 `source` / `remark` 可追溯性字段一并列入待确认。

### 结果

- 枚举取值已与 `scripts/validate_data.py` 的 `ENUMS` 逐项交叉核对，7 个枚举字段全部一致。
- 仓库内 `network_type` / `node_type` / 裸 `VHF` 已无残留
  （`data_dictionary.md` 中保留一处，是说明为何取 `VUHF` 的正文）。
- `data/schemas/*_template.csv` 与 `scripts/gen_test_data.py` 本就是当前口径，未改动。
- 数据未重新生成（本次只改文档），`gen_test_data.py` + `validate_data.py` 复跑仍为
  0 ERROR / 0 WARNING，与已提交数据零差异。

### 没有做的

上一节「四、没有做的」中的各项**均未推进**，本次只做枚举口径统一。

---

## 2026-09-08 · 第 1 周：数据字典、接口文档、基础测试数据集

负责人：yoii1
对应计划：《项目安排》第 1 周「确定数据和接口」，交付物为数据字典、接口文档、基础测试数据集、模块接口关系说明。

### 一、做了什么

#### 1. 确定规划区域

原计划未指定区域，而 DEM、地物、道路三类数据全部按区域下载，区域不定则数据采集无法开工。已定：

| 项 | 值 |
|---|---|
| 区域 | 太行山东麓（河北邢台西 — 山西左权），中心 `114.95, 37.30` |
| 尺寸 | 120 km × 120 km，UTM 50N / EPSG:32650（不跨带） |
| 规划 bbox | `114.2724, 36.7610, 115.6276, 37.8390` |
| 数据下载 bbox（外扩 20 km） | `114.0466, 36.5814, 115.8534, 38.0186` |

选此区域的原因：一个框内同时有太行山（1000–1800 m）与华北平原（50–100 m），
短波跳距环带与超短波地形遮蔽都能出效果。纯平原区域两者算出来都接近圆形，
而软需 SR-4.2.3 c 明确要求覆盖不得用规则扇形/圆形近似。

**下载必须用外扩框**。少下这 20 km，规划区西边缘的链路拿不到太行山主脊的遮挡数据，
覆盖会算得偏乐观，而西边缘恰好是山地。

#### 2. 交付文档（`docs/design/`）

| 文件 | 内容 |
|---|---|
| `01_数据字典.md` | 全局约定（坐标系、单位、ID 规则、时间格式、全局枚举、空值约定）+ 28 类实体字段定义 |
| `02_数据采集任务单.md` | 网上采集部分的任务分解，含数据源、字段表头、单位换算、验收标准，可直接转交 |
| `03_接口文档.md` | 算法后端 ↔ GIS 前端 HTTP 接口，覆盖 SR-4.2 / SR-5 / SR-6 全部接口 |
| `04_模块接口关系说明.md` | 模块职责边界、依赖关系图、两条主流程时序图、接口实现节奏表 |

#### 3. 代码（`scripts/`）

| 文件 | 说明 |
|---|---|
| `terrain.py` | 地形引擎：高程、坡度、地物、通视（含 4/3 地球曲率）、地形剖面 |
| `propagation.py` | 传播模型与链路预算：超短波刃形绕射、短波地波 + NVIS、噪声底与 SNR 余量 |
| `gen_test_data.py` | 测试数据生成，种子 `SEED = 20260908` |
| `validate_data.py` | 数据校验，7 类检查，退出码 0/1 |
| `datapaths.py` | 数据集简称到仓库路径的映射（后续调目录只改这里） |

全部纯标准库，无第三方依赖。

#### 4. 基础测试数据集（`data/synthetic/` `data/fault/` `data/dictionary/`）

| 数据 | 实际 | 口径要求 | 状态 |
|---|---|---|---|
| 节点 | 156（短波 52 / 超短波 104） | 短波 ≥50、超短波 ≥100 | ✅ |
| 网络拓扑链路 | 500（可用 480） | 150 节点以上，200–500 链路 | ✅ |
| 任务通联关系 | 303 条 / 6 个场景 | 5–10 组，每组 30–100 条 | ✅ |
| 链路状态记录 | 2500 | ≥1000 | ✅ |
| 频率资源 | 94（短波 46 / 超短波 48） | 每类 20–50 | ✅ |
| 干扰源 | 16 | 10–20 | ✅ |
| 故障案例 | 240（四类各 60） | 4 类，每类 50–100 | ✅ |
| 现象字典 | 15 条 | —— | ✅ |
| 故障场景 | 5 个（含 1 个复合场景） | —— | ✅ |

### 二、结果与验证

```
python3 scripts/gen_test_data.py     # 全部规模达标
python3 scripts/validate_data.py     # 0 ERROR，0 WARNING，退出码 0
git status                            # 重新生成后与已提交数据零差异
```

**数据可复现已验证**：固定种子重跑，输出与仓库中已提交的数据逐字节一致。

校验脚本覆盖 7 类检查：引用完整性（12 组外键）、枚举与布尔合法性（12 个枚举字段 + 6 个布尔字段）、
数值范围、拓扑连通性、通联需求可满足性、标签分布、故障场景适配性。

### 三、几个关键决策，接手时需要知道

1. **拓扑不是取距离最短的 500 条链路**。
   第一版那样做，全网清一色 `EXCELLENT`，SR-6.2 无法从链路指标挖掘状态判定规则，
   SR-5 也没有弱链路可供故障分析。现在改成：先用并查集建连通骨架保证路由有解，
   再按状态配额分层抽样。当前分布 `EXCELLENT=250 / GOOD=140 / WARNING=80 / FAULT=30`，
   链路状态记录为 `1343 / 649 / 343 / 165`。**改动 `gen_links()` 时要保住这个分布。**

2. **必要通联关系的连通性是硬性前提**。
   SR-4.2.4 要把「必要通联关系的端到端连通率」作为硬约束写进目标函数。
   若这些通联关系两端本就不在同一连通分量，多目标求解一开始就无解。
   校验脚本第 5 项专门查这一条，当前 43 条必要需求两端全部连通。

3. **链路余量用 SNR 而非接收灵敏度**。
   灵敏度余量在短距离链路上动辄 60–70 dB，无法区分链路优劣；
   SNR 余量才与误码率、链路状态直接相关。噪声底按 ITU-R P.372 口径，
   短波取外部噪声系数 40 dB、超短波取 8 dB。

4. **合成地形已按太行山东麓标定**，DEM 到位前的占位实现：
   西部山区坡度均值 19°、最大 37°，东部平原 1.2°，坡度约束能筛掉 22% 的面积；
   高程 59–1919 m。若地形过于平坦，候选点筛选的坡度约束会形同虚设。

5. **传播模型的物理表现已核对**：
   短波地波按 ITU-R P.368 平均地质曲线标定（40 km 可用 / 60 km 预警 / 120 km 失效），
   NVIS 天线 0–300 km 全程可用；超短波 VHF 50 MHz 绕山后仍为 `GOOD`（波长 6 m，
   菲涅尔区大，绕射损耗仅约 9 dB），同路径 UHF 400 MHz 为 `FAULT`。
   这些都是占位实现的合理表现，不是缺陷。

### 四、没有做的（明确列出）

#### 数据口径中仍为空的部分

| 数据 | 口径要求 | 当前 | 阻塞原因 |
|---|---|---|---|
| DEM 地形高程 | 覆盖外扩 bbox，30 m | `data/raw/dem/` 空 | 师弟采集中，预计 9/11 |
| 地物 / 土地覆盖 | 同 bbox | `data/raw/landcover/` 空 | 同上 |
| 道路 | 覆盖规划区域 | `data/raw/roads/` 空 | 同上 |
| 电台 / 天线真实型号库 | 短波 5–10、超短波 5–10 | `data/raw/equipment_docs/` 仍为示例行 | 同上 |
| 设备说明书与维修资料 | 20–50 份 | `data/raw/maintenance_docs/` 仅有模板 | 同上 |
| **故障知识图谱三元组** | **500–2000 条关系** | **`data/fault/knowledge_graph/` 空** | **未开工** |
| **故障规则** | **50–100 条规则** | **`data/fault/rules/` 空** | **未开工** |

后两项不依赖师弟的数据，是我方这条线的欠账，本体（实体类型、关系类型）已在
`docs/design/01_数据字典.md` 3.5 节定义，规则表达式语法已在 3.4 节定义，可以直接开工。

#### 其他未做

- 第 2 周及以后的算法模块（候选点筛选、部署规划、路由、频率、参数优化、多目标组网、干扰分析、故障诊断）全部未开工。
- 真实传播模型（ITU-R P.526 / P.1812 / P.533 / P.368）与真实 DEM 未接入，当前为占位实现。
  接口 `path_loss` / `link_budget` / `los_clearance` 已固定，替换时调用方不改。
- HTTP 接口层与异步任务框架未搭建，`docs/design/03_接口文档.md` 目前只是设计，无实现。
- `data/README.md` 注意事项要求「记录生成规则、随机种子」，目前只在脚本里，
  未单独成文（CLAUDE.md 中「待与 contraller 确认」的 `source` / `remark` 字段是同一件事）。

### 五、需要确认的事项

#### 1. 发现一处跨线的枚举口径冲突（**已于 2026-09-08 确认：沿用当前代码口径，见本文件最上一节**）

`data/dictionary/data_dictionary.md`（contraller 于 `d1f5aee` 提交）与
`docs/design/01_数据字典.md`、`scripts/gen_test_data.py` 的实际输出对不上：

| 概念 | contraller 的定义 | 当前代码与数据的实际口径 |
|---|---|---|
| 网络类型 | `network_type`：`HF` / **`VHF`** | `device_class`：`HF` / **`VUHF`** |
| 节点类型 | `node_type`：`fixed` / `mobile` / `relay` | `node_role`：`TASK` / `FIXED_STATION` / `RELAY` / `CANDIDATE`，另有 `mobility`：`FIXED` / `VEHICLE` / `MANPACK` |
| 状态 | `status`：`normal` / `degraded` / `fault` / `offline`（小写） | `status`：`NORMAL` / `FAULT` / `OFFLINE`（大写）；`degraded` 在本线中属于「受影响状态」`affect_state`，不是设备状态 |

按 CLAUDE.md「字段口径」一节，文档未涉及的字段以 `gen_test_data.py` 的实际输出为准，
但这份文件是 contraller 建的，**不跨线单方面改**。建议在群里确认后统一，
统一方向倾向于沿用当前代码口径（`VUHF` 比 `VHF` 准确，因为软需覆盖 30–400 MHz，
含 UHF 频段；节点角色与机动性是两个正交维度，合并成一个 `node_type` 会丢信息）。
确认后需同步三处：`gen_test_data.py`、`data/schemas/*_template.csv`、`docs/design/01_数据字典.md`。

#### 2. 待外包前端确认（见 `docs/design/03_接口文档.md` 第 5 节，共 7 项）

其中三项不早定会返工：

- **覆盖结果传输格式**：120×120 km @30 m 是 1600 万个栅格点，序列化成 JSON 约 300 MB，
  走不了接口。方案是分级等值面 GeoJSON + 带 bbox 的半透明 PNG 双通道，需确认前端能叠加图片图层。
- **坐标序**：`[lon, lat]`（GeoJSON 标准）还是 `[lat, lon]`。不确认必出错，且属于最难排查的那类错。
- **三路融合结果的展示位**：SR-5.2 要求可展开查看各路分项结论与置信度，前端需预留展开区域。

#### 3. 其他风险

- ML 路故障诊断依赖历史案例量，当前 240 条均为构造案例。
  软需 SR-5.2 d 已写明「未达启用门限时降级为规则 + 图谱两路」，
  建议按**两路融合 + 标注降级**作为第 5 周的主交付路径，ML 路作为加分项。
- `AGENTS.md` 风险提醒中「ML 层故障诊断是否纳入本次验收范围」尚未确认，与上一条相关。

### 六、下一步

按《项目安排》第 2 周（9/15–9/21）应交付「部署规划模块 + GIS 部署结果展示」。
DEM 未到之前可先做的（都跑在 `terrain.py` 上，换 DEM 不改代码）：

1. `SR-4.2.1.1` 候选部署位置筛选 —— 坡度、地物、交通可达性、站间距、通视约束
2. `SR-4.2.1.2` 移动电台数量与位置规划 —— 最少数量 / 指定数量两种模式
3. HTTP 接口层与异步任务框架
4. 故障规则库（50–100 条）与故障知识图谱三元组（500–2000 条）—— 本线欠账，不依赖外部数据

### 七、接手方式

```bash
git pull
python3 scripts/gen_test_data.py     # 重新生成全部测试数据
python3 scripts/validate_data.py     # 校验，退出码必须为 0
```

真实数据到位后的切换方式：

- 设备/天线型号库：放入 `data/raw/equipment_docs/`，重跑生成脚本自动从构造型号切到真实型号（示例行会被忽略）
- DEM：放入 `data/raw/dem/`，把 `scripts/terrain.py` 中的 `SyntheticTerrain` 换成 `DemTerrain`，其余代码不动
