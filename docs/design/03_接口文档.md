# 算法后端 ↔ GIS 前端 接口文档 v0.1

> 对应软需：SR-4.2 无线网络规划 / SR-5 故障诊断 / SR-6 知识图谱
> 字段语义、单位、枚举码值一律以 `docs/01_数据字典.md` 为准，本文档不重复定义。
> 状态：草案，待与外包方评审后定版（第 1 周，9/8–9/14）

---

## 1. 通用约定

### 1.1 基础信息

| 项 | 值 |
|---|---|
| 协议 | HTTP/1.1，`Content-Type: application/json; charset=utf-8` |
| Base URL | `http://<host>:<port>/api/v1` |
| 编码 | 一律 UTF-8，请求与响应均不做额外转义 |
| 时间 | ISO 8601 带时区，见数据字典 0.5 |
| 坐标 | EPSG:4326，`[lon, lat]` 顺序 |

### 1.2 统一响应包络

**所有**接口的响应体都是这个结构，不例外：

```json
{
  "code": 0,
  "message": "success",
  "data": { },
  "request_id": "req-20260908-0001"
}
```

- `code = 0` 表示成功，非 0 表示失败，此时 `data` 为 `null`，`message` 为可直接展示给用户的中文提示
- `request_id` 全链路透传，排查问题时前后端对同一个 ID
- **HTTP 状态码只表达传输层语义**（200/202/4xx/5xx），业务成败一律看 `code`。前端不要依据 HTTP 200 判断业务成功

### 1.3 错误码

| code | 含义 | 前端建议处理 |
|---|---|---|
| 0 | 成功 | |
| 1001 | 参数缺失或格式错误 | 表单标红，`message` 指明字段 |
| 1002 | 引用的实体不存在（node_id / link_id 等） | 提示数据不一致，建议刷新 |
| 1003 | 枚举值非法 | 前端自查，属于对接缺陷 |
| 2001 | 任务不存在或已过期 | 结果保留 24h，超时提示重新计算 |
| 2002 | 任务仍在计算中 | 继续轮询 |
| 2003 | 任务计算失败 | 展示 `data.error_detail` |
| 3001 | 约束过严，无可行解 | **展示 `data.suggestions` 中的放宽建议**（SR-4.2.1.1、SR-4.2.4 扩展流要求） |
| 3002 | 频率资源不足 | 展示 `data.gap` 频率缺口统计（SR-4.2.2.2 扩展流要求） |
| 3003 | 故障对象与故障类型不适配 | 阻止保存并提示（SR-5.1 扩展流要求） |
| 4001 | 知识图谱无匹配结果 | 展示 `data.similar` 相似知识推荐（SR-6 扩展流要求） |
| 5001 | 服务内部错误 | 提示稍后重试 |

> 3001 / 3002 / 4001 这三个不是"错误"，是软需明确要求要**带建议返回**的业务分支。前端必须渲染 `data` 里的建议内容，不能只弹一句"失败"。

### 1.4 异步任务模式（SR-4.2 g 强制要求）

所有**规划与分析类计算**一律异步。规则：

```
POST  /plan/xxx           提交任务  → HTTP 202 + { task_id, estimated_sec }
GET   /tasks/{task_id}    查询进度  → { status, progress, stage }
GET   /tasks/{task_id}/result   取结果 → 各接口自有结构
POST  回调（可选）         算完主动推 → 前端提供 callback_url
```

任务状态机：

```
PENDING → RUNNING → SUCCESS
                  ↘ FAILED
                  ↘ CANCELLED
```

**`GET /tasks/{task_id}`**

```json
{
  "code": 0,
  "data": {
    "task_id": "tsk-20260908-0007",
    "task_type": "DEPLOYMENT_PLAN",
    "status": "RUNNING",
    "progress": 0.62,
    "stage": "正在评估候选点覆盖增益 (312/500)",
    "submit_time": "2026-09-08T14:30:00+08:00",
    "estimated_remain_sec": 45,
    "error_detail": null
  }
}
```

- `progress` 为 0–1 浮点，`stage` 是可直接展示的中文进度描述
- **建议轮询间隔 2 s**，前端不要低于 1 s
- 结果保留 **24 小时**
- `DELETE /tasks/{task_id}` 取消任务

> 软需 SR-4.2 g 明确写了"响应时间指标仅适用于参数录入、方案查看等界面交互操作，不含算法计算耗时"。这条要在验收时用上：**规划计算耗时不受 2 秒响应时间指标约束**，但界面交互类接口受约束。

---

## 2. SR-4.2 无线网络规划接口

### 2.1 候选部署位置筛选 `SR-4.2.1.1`

**`POST /plan/candidate-sites`** → 异步

请求：

```json
{
  "task_id": "TS-0001",
  "area_bbox": [114.2724, 36.7610, 115.6276, 37.8390],
  "constraints": {
    "max_slope_deg": 15,
    "elevation_range_m": [100, 1500],
    "allowed_landcover": ["草地", "耕地", "裸地"],
    "road_max_distance_m": 2000,
    "min_site_spacing_m": 3000,
    "los_required_node_ids": ["ND-0003", "ND-0012"],
    "grid_step_m": 500
  },
  "forced_include": [{"lon": 114.88, "lat": 37.21}],
  "forced_exclude_polygons": [],
  "callback_url": null
}
```

约束项说明：

| 字段 | 对应软需 | 缺省 |
|---|---|---|
| `max_slope_deg` | SR-4.2.1.1 a 地形坡度 | 15 |
| `allowed_landcover` | SR-4.2.1.1 a 地物类型 | 全部除水域/建成区 |
| `road_max_distance_m` | SR-4.2.1.1 a 交通可达性 | 2000 |
| `min_site_spacing_m` | SR-4.2.1.1 b 最小站间距 | 3000 |
| `elevation_range_m` | SR-4.2.1.1 b 海拔范围 | 不限 |
| `los_required_node_ids` | SR-4.2.1.1 b 通视要求 | 空 |

结果 `GET /tasks/{id}/result`：

```json
{
  "code": 0,
  "data": {
    "task_id": "tsk-...",
    "candidate_count": 428,
    "candidates": [
      {
        "site_id": "CP-0001",
        "lon": 114.8812, "lat": 37.2103,
        "elevation_m": 862.4,
        "slope_deg": 6.2,
        "landcover": "草地",
        "road_distance_m": 740,
        "visible_node_count": 17,
        "score": 0.82
      }
    ],
    "rejected_stats": {
      "slope": 12043, "landcover": 8871, "road": 3320, "spacing": 1502
    }
  }
}
```

> `rejected_stats` 是给 SR-4.2.1.1 扩展流"候选点过少时提示约束过严"用的 —— 前端可以直接告诉用户是哪一条约束筛掉得最多。候选点数 < 20 时后端会同时返回 `code=3001` 与 `suggestions`。

### 2.2 移动电台数量与位置规划 `SR-4.2.1.2`

**`POST /plan/deployment`** → 异步

```json
{
  "task_id": "TS-0001",
  "candidate_task_id": "tsk-...",
  "existing_node_ids": ["ND-0001", "ND-0002"],
  "comm_demand_ids": ["CD-0001", "CD-0002"],
  "mode": "MIN_COUNT",
  "fixed_count": null,
  "coverage_target": {
    "min_link_margin_db": 6,
    "required_coverage_ratio": 0.95
  },
  "device_model_id": "DM-0001",
  "solution_count": 3
}
```

- `mode = MIN_COUNT`：自动求满足通联需求的**最少电台数**（SR-4.2.1.2 b 最经济方案）
- `mode = FIXED_COUNT`：用户指定 `fixed_count`，求该数量下的最优位置（SR-4.2.1.2 c）
- `solution_count`：生成几套备选方案（SR-4.2.1.2 扩展流"多组备选方案对比"）

结果：

```json
{
  "data": {
    "solutions": [
      {
        "solution_id": "PL-0001",
        "radio_count": 7,
        "deployments": [
          {"site_id": "CP-0103", "lon": 114.71, "lat": 37.44,
           "node_id_assigned": "ND-0151", "device_model_id": "DM-0001",
           "covers_demand_ids": ["CD-0001", "CD-0007"]}
        ],
        "metrics": {
          "coverage_ratio": 0.962,
          "satisfied_demand_count": 48,
          "total_demand_count": 50,
          "deploy_cost": 7
        },
        "blind_zones": [
          {"geometry": {"type": "Polygon", "coordinates": [[[114.3,36.9],[114.4,36.9],[114.4,37.0],[114.3,36.9]]]},
           "area_km2": 62.1,
           "affected_demand_ids": ["CD-0031", "CD-0044"],
           "suggestion": "该区域被太行山主脊遮挡，建议在 CP-0288 增设 1 部中继电台"}
        ]
      }
    ]
  }
}
```

> `blind_zones` 对应 SR-4.2.1 扩展流"标注盲区范围并给出增加部署数量的建议"，前端按半透明多边形渲染。

### 2.3 路由规划 `SR-4.2.2.1`

**`POST /plan/route`** → 异步

```json
{
  "task_id": "TS-0001",
  "deployment_solution_id": "PL-0001",
  "demands": [
    {"demand_id": "CD-0001", "strategy": "MAX_RELIABILITY", "need_backup": true}
  ],
  "manual_paths": [
    {"demand_id": "CD-0005", "node_path": ["ND-0003","ND-0151","ND-0022"]}
  ]
}
```

`strategy` 三选一：`SHORTEST_PATH` / `MIN_DELAY` / `MAX_RELIABILITY`（SR-4.2.2.1 a）。
`manual_paths` 对应 SR-4.2.2.1 b 人工指定路由。

结果：

```json
{
  "data": {
    "routes": [
      {
        "demand_id": "CD-0001",
        "primary": {
          "node_path": ["ND-0003", "ND-0151", "ND-0022"],
          "link_path": ["LK-0210", "LK-0398"],
          "hop_count": 2,
          "total_delay_ms": 84.2,
          "min_link_margin_db": 8.6,
          "reliability": 0.983,
          "bottleneck_link_id": "LK-0398"
        },
        "backup": { "node_path": ["..."], "reliability": 0.941 },
        "reachable": true
      }
    ],
    "unreachable_demand_ids": ["CD-0044"]
  }
}
```

### 2.4 频率资源分配 `SR-4.2.2.2`

**`POST /plan/frequency`** → 异步

```json
{
  "route_task_id": "tsk-...",
  "freq_pool_filter": {
    "device_class": "HF",
    "freq_range_khz": [3000, 15000],
    "exclude_freq_ids": ["FQ-0007"]
  },
  "reuse_min_distance_m": 25000,
  "adjacent_guard_khz": 3,
  "manual_assignments": [{"link_id": "LK-0210", "freq_khz": 7185}]
}
```

结果：

```json
{
  "data": {
    "assignments": [
      {"link_id": "LK-0210", "freq_id": "FQ-0012", "freq_khz": 7185,
       "bandwidth_khz": 3, "reuse_group": 2}
    ],
    "conflicts": [
      {"type": "CO_CHANNEL", "link_ids": ["LK-0310","LK-0455"],
       "freq_khz": 8420, "distance_m": 18300,
       "required_distance_m": 25000, "severity": "SEVERE"}
    ],
    "gap": {"required_channels": 62, "available_channels": 50, "shortage": 12},
    "conflict_free": false
  }
}
```

> 冲突无法完全避免时返回 `code=3002` + `gap`，对应 SR-4.2.2.2 扩展流"提示冲突无法完全避免并给出频率缺口统计"。前端要把 `gap` 渲染出来，不能只报失败。

### 2.5 电台参数规划与覆盖计算 `SR-4.2.3`

**`POST /plan/radio-params`** → 异步

```json
{
  "deployment_solution_id": "PL-0001",
  "optimize_vars": ["TX_POWER", "ANTENNA_HEIGHT", "TILT"],
  "bounds": {
    "tx_power_dbm": [30, 50],
    "antenna_height_m": [2, 12],
    "tilt_deg": [-10, 10]
  },
  "coverage_target": {"min_field_strength_dbuvm": 20, "target_radius_m": 30000},
  "manual_override": [{"device_id": "DV-0113", "tx_power_dbm": 43}]
}
```

**`POST /plan/coverage`** → 异步，计算覆盖热力图与场强分布（SR-4.2.3 c）

```json
{
  "device_ids": ["DV-0113"],
  "param_task_id": "tsk-...",
  "grid_step_m": 200,
  "output": ["CONTOUR", "RASTER_PNG"],
  "time_of_day": "2026-09-08T14:00:00+08:00"
}
```

结果（**注意传输格式，见下方说明**）：

```json
{
  "data": {
    "device_id": "DV-0113",
    "model_used": "HF_SKYWAVE_P533 + GROUNDWAVE",
    "bbox": [114.05, 36.58, 115.85, 38.02],
    "contour": {
      "type": "FeatureCollection",
      "features": [
        {"type": "Feature",
         "properties": {"level": "STRONG", "field_strength_dbuvm_min": 40},
         "geometry": {"type": "MultiPolygon", "coordinates": [[[[114.9,37.2],[115.0,37.2],[115.0,37.3],[114.9,37.2]]]]}}
      ]
    },
    "raster_png": {
      "url": "/api/v1/files/cov_tsk-xxx.png",
      "bbox": [114.05, 36.58, 115.85, 38.02],
      "width": 1800, "height": 1440,
      "colormap": [{"value_dbuvm": 0, "rgba": [0,0,0,0]},
                   {"value_dbuvm": 20, "rgba": [255,214,0,140]},
                   {"value_dbuvm": 40, "rgba": [0,160,80,160]}]
    },
    "hf_structure": {
      "ground_wave_radius_m": 48000,
      "skip_zone": {"inner_radius_m": 48000, "outer_radius_m": 305000},
      "hops": [{"hop": 1, "inner_m": 305000, "outer_m": 820000}]
    }
  }
}
```

> **这是本文档最需要外包方确认的一处。**
>
> 规划区 120×120 km，按 30 m 栅格是 4000×4000 = **1600 万个点**，序列化成 JSON 约 300 MB —— 不可能走接口传。所以覆盖结果按两种形式给：
>
> 1. **`contour`**：按强/中/弱分级的等值面 GeoJSON MultiPolygon，量级几百 KB，**前端矢量渲染用这个**，可点击、可查属性；
> 2. **`raster_png`**：带 `bbox` 的半透明 PNG，前端按图片图层叠加，用于平滑的热力图观感。
>
> 两者同时返回，前端按缩放级别取舍（小比例尺用 PNG，放大后叠 contour，对应 SR-1.1.2.6.2 扩展流"全域小比例尺简化显示，放大呈现精细边界"）。
>
> `hf_structure` 是短波专有字段：软需 SR-4.2.3 c / SR-1.1.2.6.2 a 明确要求短波"按跳距与静区结构呈现环带形态，不采用规则扇形/圆形近似"。地波半径、静区内外径、各跳环带单独给出，前端可据此画环带，也可直接用 `contour`（已含环带形状）。**超短波无此字段**，其不规则边界完全由 `contour` 表达。

### 2.6 多目标优化组网 `SR-4.2.4`

**`POST /plan/network-optimize`** → 异步

```json
{
  "deployment_solution_id": "PL-0001",
  "frequency_task_id": "tsk-...",
  "mandatory_demand_ids": ["CD-0001", "CD-0003", "CD-0008"],
  "objectives": ["MAX_CONNECTIVITY", "MIN_TOTAL_POWER", "MAX_SURVIVABILITY"],
  "hard_constraints": {
    "mandatory_e2e_connectivity": 1.0,
    "survivability_threshold": 0.90,
    "max_relay_hops": 2
  },
  "locked_links": ["LK-0210"],
  "locked_relay_sites": ["CP-0103"],
  "pareto_size": 10
}
```

硬约束直接对应甲方要求与 SR-4.2.4 a/b/d：端到端连通率为硬约束、中继跳数 ≤ 2、单链路断开时关键通联连通率 ≥ 90%。

结果：

```json
{
  "data": {
    "feasible": true,
    "pareto_solutions": [
      {
        "solution_id": "PL-0011",
        "rank": 1,
        "objectives": {
          "e2e_connectivity": 1.0,
          "total_radiated_power_dbm": 61.3,
          "survivability": 0.94
        },
        "added_relays": [
          {"site_id": "CP-0288", "lon": 114.62, "lat": 37.55, "hops_saved": 1}
        ],
        "selected_link_ids": ["LK-0210", "LK-0398"],
        "constraint_check": {
          "mandatory_e2e_connectivity": {"value": 1.0, "required": 1.0, "pass": true},
          "survivability": {"value": 0.94, "required": 0.90, "pass": true},
          "max_relay_hops": {"value": 2, "required": 2, "pass": true}
        }
      }
    ],
    "infeasible_reason": null,
    "suggestions": []
  }
}
```

> 无可行解时返回 `code=3001`，`infeasible_reason` 指明是哪条硬约束冲突，`suggestions` 给放宽建议（SR-4.2.4 扩展流 1）。
> `total_radiated_power_dbm` 是**全网总辐射功率**，dBm 域求和需先转线性再转回，后端已处理，前端直接展示。

### 2.7 干扰影响分析与抗干扰推荐 `SR-4.2.5`

**`POST /interference/analyze`** → 异步

```json
{
  "network_solution_id": "PL-0011",
  "interference_ids": ["IF-0001", "IF-0003"],
  "recommend": true
}
```

结果：

```json
{
  "data": {
    "affected_area": {
      "type": "FeatureCollection",
      "features": [{"type":"Feature",
        "properties": {"level":"SEVERE","interference_ids":["IF-0001"]},
        "geometry": {"type":"Polygon","coordinates":[[[114.5,37.1],[114.7,37.1],[114.7,37.3],[114.5,37.1]]]}}]
    },
    "affected_nodes": [
      {"node_id": "ND-0055", "level": "SEVERE",
       "snr_before_db": 18.2, "snr_after_db": 3.1, "delta_db": -15.1}
    ],
    "affected_links": [
      {"link_id": "LK-0398", "level": "MODERATE",
       "snr_before_db": 15.0, "snr_after_db": 8.4,
       "state_before": "GOOD", "state_after": "WARNING",
       "interference_id": "IF-0001"}
    ],
    "countermeasures": [
      {
        "type": "FREQ_ADJUST",
        "target": {"link_id": "LK-0398"},
        "detail": {"from_freq_khz": 8420, "to_freq_khz": 9105},
        "priority": 1,
        "pre_eval": {
          "snr_after_db": 14.6,
          "affected_link_count_before": 12,
          "affected_link_count_after": 3
        }
      },
      {"type": "NODE_REDEPLOY", "target": {"node_id": "ND-0055"},
       "detail": {"to_site_id": "CP-0417"}, "priority": 4, "pre_eval": {}}
    ]
  }
}
```

> `countermeasures` 的 `type` 必须覆盖软需 SR-4.2.5 c 要求的五类：`FREQ_ADJUST` / `CHANNEL_SWITCH` / `POWER_OPT` / `ROUTE_REBUILD` / `NODE_REDEPLOY`。
> `pre_eval` 是措施的**效果预评估**（SR-4.2.5 主事件流 6："输出实施前后的受扰范围与链路质量对比"），前端做前后对比展示。
> 多干扰源按**功率线性域叠加**统一计算，不重复计数（SR-4.2.5 扩展流 1）。

**`POST /interference/adopt`** —— 用户采纳某条措施，后端同步更新频率/功率/路由/部署配置（SR-4.2.5 主事件流 7）。同步接口，≤2 s。

### 2.8 方案管理

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/plans?task_id=TS-0001` | 方案列表 |
| GET | `/plans/{solution_id}` | 方案详情（部署+路由+频率+参数+指标） |
| POST | `/plans/{solution_id}/compare` | 多方案横向对比，body 传 `solution_ids[]` |
| POST | `/plans/{solution_id}/confirm` | 方案转正，生成正式配置 |
| GET | `/plans/{solution_id}/export?format=xlsx\|json` | 导出 |

---

## 3. SR-5 故障诊断接口

### 3.1 故障场景 `SR-5.1`

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/fault/scenarios` | 新建故障场景（手动标绘） |
| PUT | `/fault/scenarios/{id}` | 编辑 |
| GET | `/fault/scenarios` | 列表，支持复用 |
| DELETE | `/fault/scenarios/{id}` | 删除（撤销标绘） |
| POST | `/fault/scenarios/import` | 批量导入，`multipart/form-data` 传 CSV/XLSX |
| POST | `/fault/scenarios/validate` | **适配性校验**，保存前调用 |

新建请求体即数据字典 3.6 的 `fault_scenario`：

```json
{
  "scenario_name": "太行西侧中继站硬件故障",
  "faults": [
    {"object_type": "DEVICE", "object_id": "DV-0113",
     "fault_type": "HW_FAILURE",
     "symptom_ids": ["SY-0003", "SY-0011"],
     "occur_time": "2026-09-08T09:15:00+08:00",
     "params": {}}
  ]
}
```

校验不通过返回 `code=3003`：

```json
{"code": 3003,
 "message": "链路中断故障不能作用于设备对象，请选择链路",
 "data": {"index": 0, "object_type": "DEVICE", "fault_type": "LINK_DOWN"}}
```

> 适配规则见数据字典 3.6 末尾。前端应在保存前调 `validate`，对应 SR-5.1 扩展流 3"不适配时提示并阻止保存"。

### 3.2 故障推理定位 `SR-5.2` —— 三路融合

**`POST /fault/diagnose`** → 异步

```json
{
  "scenario_id": "FS-0001",
  "network_solution_id": "PL-0011",
  "fusion_weights": {"RULE": 0.35, "KG": 0.35, "ML": 0.30},
  "confidence_threshold": 0.6
}
```

结果 —— **结构必须支持逐路展开**（SR-5.2 e/f）：

```json
{
  "data": {
    "scenario_id": "FS-0001",
    "fusion_mode": "THREE_WAY",
    "results": [
      {
        "fault_object": {"object_type": "DEVICE", "object_id": "DV-0113",
                         "node_id": "ND-0151"},
        "fault_component": "功率放大模块",
        "fused": {
          "fault_type": "HW_FAILURE",
          "confidence": 0.87,
          "root_cause": "功放模块输出级晶体管击穿",
          "need_manual_review": false
        },
        "branches": [
          {"source": "RULE", "fault_type": "HW_FAILURE", "confidence": 0.90,
           "evidence": {"rule_ids": ["FR-0007","FR-0021"],
                        "matched_expr": "\"驻波比告警\" in symptom_ids and tx_power_dbm > 0"}},
          {"source": "KG", "fault_type": "HW_FAILURE", "confidence": 0.85,
           "evidence": {"path": ["驻波比告警 -hasSymptom- 功放失效",
                                 "功放失效 -locatedAt- 功率放大模块"],
                        "triple_ids": ["KG-0231","KG-0788"]}},
          {"source": "ML", "fault_type": "HW_FAILURE", "confidence": 0.86,
           "evidence": {"model": "xgboost_v1", "top_features":
                        [{"name":"device_selftest","contribution":0.41},
                         {"name":"snr_db","contribution":0.19}]}}
        ],
        "candidates": [
          {"fault_type": "HW_FAILURE", "confidence": 0.87},
          {"fault_type": "SW_CONFIG",  "confidence": 0.08}
        ]
      }
    ]
  }
}
```

关键约定：

- `fusion_mode` 取 `THREE_WAY` 或 **`TWO_WAY_DEGRADED`**。ML 路的历史案例量未达启用门限时自动降级为规则+图谱两路，**前端必须把降级状态显示出来**（SR-5.2 d/扩展流 1 明确要求"在结果中标注当前融合路数"）
- 降级时 `branches` 里 ML 项仍存在，但 `confidence: null` 且带 `"disabled_reason": "历史案例 137 条，未达门限 400 条"`
- `candidates` 按置信度降序，三路结论不一致时前端展示候选列表（扩展流 2）
- `fused.confidence < confidence_threshold` 时 `need_manual_review = true`，前端提示人工研判（扩展流 3）
- `branches[].evidence` 是**判定依据**，前端做展开查看（SR-5.2 主事件流 6）。三路的 evidence 结构不同，前端按 `source` 分别渲染

### 3.3 故障影响分析 `SR-5.3`

**`POST /fault/impact`** → 异步

```json
{
  "diagnose_task_id": "tsk-...",
  "network_solution_id": "PL-0011",
  "consider_backup_route": true
}
```

结果：

```json
{
  "data": {
    "impact_scope": {
      "type": "FeatureCollection",
      "features": [{"type":"Feature",
        "properties": {"level":"SEVERE","fault_object_id":"DV-0113"},
        "geometry": {"type":"Polygon","coordinates":[[[114.6,37.4],[114.8,37.4],[114.8,37.6],[114.6,37.4]]]}}]
    },
    "affected_nodes": [
      {"node_id": "ND-0022", "affect_type": "DIRECT", "level": "SEVERE",
       "state": "INTERRUPTED", "node_role": "TASK", "region": "西区",
       "reason": "与故障节点 ND-0151 直连且无备用路径"},
      {"node_id": "ND-0088", "affect_type": "INDIRECT", "level": "MODERATE",
       "state": "DEGRADED", "detour_route": ["ND-0088","ND-0034","ND-0022"],
       "reason": "原路由经由故障节点，已迂回至备用路径，时延增加 42 ms"}
    ],
    "affected_links": [
      {"link_id": "LK-0398", "state": "INTERRUPTED", "level": "SEVERE",
       "related_fault_object_id": "DV-0113",
       "bandwidth_loss_kbps": 64, "is_backbone": true,
       "reason": "链路一端设备故障"}
    ],
    "summary": {
      "affected_node_count": 9,
      "direct_count": 2, "indirect_count": 7,
      "interrupted_node_count": 2, "degraded_node_count": 7,
      "affected_link_count": 5,
      "total_bandwidth_loss_kbps": 224,
      "affected_mandatory_demand_ids": ["CD-0003"]
    }
  }
}
```

- `affect_type`：`DIRECT` 直接受影响 / `INDIRECT` 间接受影响（SR-5.3 b）
- `state`：`INTERRUPTED` 业务中断 / `DEGRADED` 降级运行 / `NORMAL`（SR-5.3 扩展流 4）
- `level`：`SEVERE` / `MODERATE` / `MINOR` 三级，前端按颜色区分（SR-5.3 e）
- `is_backbone = true` 的链路影响等级自动提升一级（SR-5.3 扩展流 5）
- 多故障点时 `impact_scope.features` 按 `fault_object_id` 分组，前端可单独显隐、可叠加（扩展流 6）

**`GET /fault/impact/{task_id}/export?format=xlsx|png`** —— 清单导出表格、图层导出图片（SR-5.3 扩展流 8）。

### 3.4 修复建议与诊断报告 `SR-5.4`

**`GET /fault/suggestions?diagnose_task_id=tsk-...`** —— 同步，≤2 s

```json
{
  "data": {
    "suggestions": [
      {"priority": 1, "action": "更换功率放大模块",
       "steps": ["断电并断开天线馈线","拆卸机箱上盖","更换功放模块 PA-200","加电自检并测量驻波比"],
       "required_spares": ["功放模块 PA-200"],
       "required_tools": ["万用表","驻波比测试仪"],
       "estimated_time_min": 90,
       "kg_source_triple_ids": ["KG-0788"],
       "similar_cases": [
         {"case_id": "FC-0031", "similarity": 0.91, "solution": "更换功放模块", "repair_time_min": 85}
       ]},
      {"priority": 2, "action": "启用备份电台并切换路由", "estimated_time_min": 15}
    ]
  }
}
```

**`POST /fault/report`** —— 生成诊断报告

```json
{"diagnose_task_id": "tsk-...", "impact_task_id": "tsk-...", "format": "docx"}
```

返回 `{"report_url": "/api/v1/files/report_FS-0001.docx"}`。格式支持 `docx` / `pdf` / `json`。报告含：故障信息、根因（含三路分项置信度）、影响范围、修复建议四部分（SR-5.4 c）。

---

## 4. SR-6 知识图谱接口

| 方法 | 路径 | 说明 | 对应 |
|---|---|---|---|
| GET | `/kg/entity/{entity_id}` | 实体详情 + 一跳邻居 | SR-6.1 c |
| GET | `/kg/search?q=&type=&limit=` | 模糊检索实体 | SR-6.1 扩展流 1 |
| POST | `/kg/query` | 结构化子图查询 | SR-6 c |
| POST | `/kg/infer` | 图谱推理 | SR-6.3 d |
| GET | `/kg/device/{model_id}/knowledge` | 设备知识面板数据 | SR-6.1 |
| POST | `/kg/link-state/evaluate` | 链路状态评估 | SR-6.2 d |
| POST | `/kg/entities` `/kg/triples` | 增量更新（需审核） | SR-6 d |
| POST | `/kg/versions/{v}/rollback` | 版本回滚 | SR-6 扩展流 2 |

**`POST /kg/link-state/evaluate`** —— SR-6.2 的核心对外能力，同步：

```json
{"metrics": [{"link_id":"LK-0398","snr_db":8.4,"ber":2.1e-4,
              "packet_loss_rate":0.06,"delay_ms":95,"throughput_kbps":38}]}
```

```json
{"data": {"results": [
  {"link_id": "LK-0398", "link_state": "WARNING", "confidence": 0.88,
   "matched_rules": ["LR-0012"],
   "dominant_metric": "snr_db",
   "explanation": "信噪比 8.4 dB 低于良好门限 12 dB，丢包率 6% 超预警线 5%"}]}}
```

查询无匹配时返回 `code=4001` + `data.similar`（SR-6 扩展流 1）。

---

## 5. 待外包方确认事项

| # | 事项 | 说明 |
|---|---|---|
| 1 | **覆盖结果传输格式** | 见 2.5 说明。栅格不可能走 JSON，需确认前端能否接受 `contour` GeoJSON + `raster_png` 双通道，以及 PNG 图层叠加是否可行 |
| 2 | **异步任务轮询 or 回调** | 前端倾向轮询还是提供 `callback_url`？回调需前端有可被后端访问的地址 |
| 3 | 文件下载方式 | 报告、导出文件走 `/files/{name}` 静态下载，还是需要签名 URL |
| 4 | 鉴权方式 | 是否接入统一登录（SR-7 用户管理），Header 用 `Authorization: Bearer <token>` 还是内网免鉴权 |
| 5 | 坐标序 | 确认前端 GIS 组件用 `[lon, lat]`（GeoJSON 标准）还是 `[lat, lon]`。**此项不确认必出错** |
| 6 | 三路融合结果的展示位 | SR-5.2 要求可展开查看分项，需确认前端预留了展开区域 |
| 7 | 大数据量分页 | 节点/链路列表接口是否需要分页，还是一次性全量拉取（150 节点 / 500 链路量级可全量） |
