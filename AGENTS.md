# AGENTS

本仓库用于编写《短波/超短波智能规划系统软件需求规格说明书》的部分章节。

## 当前工作范围

本次仅编写以下七个章节：

- `3.1 要求的状态和方式`
- `3.2 CSCI能力需求`
- `3.3 CSCI软件外部接口需求`
- `3.4 软件内部接口需求`
- `3.5 CSCI内部数据需求`
- `3.11 计算机资源需求`
- `3.12 设计和实现约束`

除上述七章外，不得擅自扩写其他正式章节正文。

## 输入资料

当前仓库中的项目原始资料尚未迁移到 `docs/input/`，初始化阶段应将仓库根目录下以下文件视为输入资料：

- `1111 2.docx`
- `【非密】短波规划要求.docx`
- `五大模块技术详细方案.docx`
- `半年落地问题清单.docx`
- `软件需求规格说明模板(1).docx`

后续如果将原始资料迁移至 `docs/input/`，应优先读取 `docs/input/` 下资料，并保持与上述原件一致。

## 写作规则

- 本文档是软件需求规格说明书，不是技术方案、详细设计、论文或汇报材料。
- 正文需求优先使用“软件应……”句式。
- 每条需求应尽量满足可验证、可追踪、可审查。
- 不得把实现技术写成需求主体。实现技术仅可在 `3.12 设计和实现约束` 中作为约束描述。
- 不得使用“友好、智能、高效、完善、较好、尽量、可能、等等、和/或”等模糊表述。
- 对不确定内容统一标注“待确认”，不得编造设备型号、接口协议、部署地点、CPU 型号、故障案例数量等信息。
- 当前已确认的验收口径为：短波节点数量不少于 50 个、超短波节点数量不少于 100 个、地图最大范围不小于 120 km x 120 km。
- 对上述三项指标，如原始资料存在更低口径或差异表述，当前以用户确认口径为准，不再作为扩展设计目标或待确认事项处理。
- 《五大模块技术详细方案》中的其他更高指标仅可作为扩展设计目标、背景说明或待确认约束，不得直接写成默认验收要求。
- `半年落地问题清单.docx` 中的高风险事项必须进入风险控制与范围控制规则。

## 需求编号与条目格式

章节编号规则如下：

- `SR-3.1-001`
- `SR-3.2-001`
- `SR-3.3-001`
- `SR-3.4-001`
- `SR-3.5-001`
- `SR-3.11-001`
- `SR-3.12-001`

每条正式需求建议包含以下字段：

- `需求编号`
- `需求内容`
- `合格性方法：演示 / 测试 / 分析 / 审查`
- `追踪来源`

## 章节边界控制

- `3.1` 只定义运行状态、运行方式及其对应关系，不展开详细功能设计。
- `3.2` 只写作为验收条件的能力需求，不写实现算法、技术选型和架构展开。
- `3.3` 只写与外部实体交换数据的接口需求；接口协议不明时写“待确认”。
- `3.4` 只写软件内部模块间接口需求，不把详细设计、类结构、微服务拆分写成硬性需求。
- `3.5` 只写内部数据、数据类别、存储对象、保留要求、一致性和约束，不写数据库实现细节作为主体需求。
- `3.11` 只写计算机资源需求，包括处理、存储、并发、时延、刷新周期等可验证资源需求。
- `3.12` 只写需方或项目资料明确施加的设计与实现约束，以及必须保留的“待确认”限制条件。

## 风险提醒

以下事项在写作中必须保留风险标识，未经确认不得写成确定性要求：

- 知识图谱训练语料来源与规模
- 设备状态上报协议及接口文档
- 国产化部署平台 CPU 架构
- DEM 地形数据来源与范围
- 课题组与合作方的分工边界
- Python 与 Java 的接口规范
- ML 层故障诊断是否纳入本次验收范围

## 产出位置

- 章节初稿放入 `docs/draft/`
- 合并后的输出稿放入 `docs/output/`
- 写作辅助规则放入 `skills/`
